export const lessonMetadata = {
  domainId: 'mlops-llmops-production-systems',
  id: 'compute-layer-design-quiz',
  locale: 'vi',
  title: "Quiz: Tầng Tính toán",
  headings: [
    'Quy trình Huấn luyện Phân tán DDP',
    'Lựa chọn Chiến lược Tính toán Phân tán',
    'Khắc phục Nghẽn Cổ chai GPU Under-utilization',
    'Tối ưu Chi phí Hạ tầng Doanh nghiệp',
    'Phân định Vai trò: Kubernetes vs Ray',
  ],
  keywords: ['MLOps', 'Quiz', 'Compute Layer', 'PyTorch DDP', 'FSDP', 'Ray', 'Kubernetes', 'Spot Instances'],
  pageCount: 5,
}

<MdxQuiz id="compute-layer-design-quiz" questions={[
  {
    id: 'ddp-step-order',
    title: 'Quy trình Huấn luyện Phân tán DDP',
    prompt: 'Hãy sắp xếp các bước sau theo đúng quy trình thực thi một epoch huấn luyện phân tán với PyTorch DDP:',
    mode: 'order',
    options: [
      { id: 'ddp-forward', label: 'Mỗi GPU tính toán Forward pass trên mini-batch riêng để tính giá trị Loss cục bộ' },
      { id: 'ddp-backward', label: 'Tính toán Backward pass để sinh ra các Gradient cục bộ trên từng card GPU' },
      { id: 'ddp-step', label: 'Optimizer cập nhật trọng số mô hình đồng bộ trên toàn bộ các GPU' },
      { id: 'ddp-allreduce', label: 'Thực thi thuật toán Ring-AllReduce qua giao tiếp NCCL để tính Gradient trung bình toàn cục' },
    ],
    correctOrder: [
      'ddp-forward',
      'ddp-backward',
      'ddp-allreduce',
      'ddp-step',
    ],
    success: 'Chính xác! Thứ tự chuẩn trong DDP: Forward pass -> Backward pass -> AllReduce đồng bộ Gradient -> Optimizer update.',
    error: 'Chưa đúng. AllReduce phải diễn ra sau Backward pass và trước khi Optimizer cập nhật trọng số.',
  },
  {
    id: 'compute-strategy-categorize',
    title: 'Lựa chọn Chiến lược Tính toán Phân tán',
    prompt: 'Phân loại từng tình huống kỹ thuật sau vào đúng chiến lược tính toán tối ưu:',
    mode: 'categorize',
    oneByOne: true,
    categories: [
      { id: 'c-ddp', label: 'Data Parallelism (PyTorch DDP / Ray Train)' },
      { id: 'c-fsdp', label: 'Model/Tensor Sharding (FSDP / DeepSpeed)' },
      { id: 'c-spot', label: 'Elastic Spot Orchestration' },
    ],
    options: [
      { id: 'opt-c1', label: 'Mô hình ResNet-50 vừa vặn trong VRAM nhưng dataset có 10 triệu ảnh cần tăng tốc train', categoryId: 'c-ddp' },
      { id: 'opt-c2', label: 'Huấn luyện LLM 70B tham số có dung lượng vượt quá VRAM của 1 GPU đơn lẻ', categoryId: 'c-fsdp' },
      { id: 'opt-c3', label: 'Tác vụ chạy thử nghiệm siêu tham số dài ngày cần giảm 70% ngân sách điện toán đám mây', categoryId: 'c-spot' },
      { id: 'opt-c4', label: 'Mỗi GPU giữ một bản sao hoàn chỉnh của mô hình và xử lý một phần dữ liệu độc lập', categoryId: 'c-ddp' },
      { id: 'opt-c5', label: 'Phân chia trọng số mô hình, gradient và optimizer states qua nhiều card GPU (Zero Redundancy)', categoryId: 'c-fsdp' },
      { id: 'opt-c6', label: 'Hệ thống tự động bật máy GPU khi có job và hủy máy ngay khi hoàn tất kèm lưu checkpoint định kỳ', categoryId: 'c-spot' },
    ],
    success: 'Xuất sắc! Bạn đã phân biệt chính xác khi nào dùng DDP, khi nào cần FSDP/DeepSpeed và khi nào dùng Elastic Spot.',
    error: 'Hãy xem lại: DDP dùng khi model vừa VRAM; FSDP dùng khi model quá lớn vượt VRAM; Spot dùng để tối ưu chi phí hạ tầng.',
  },
  {
    id: 'gpu-underutilization-causes',
    title: 'Khắc phục Nghẽn Cổ chai GPU Under-utilization',
    prompt: 'Khi giám sát bằng `nvidia-smi` thấy GPU Utilization chỉ đạt 15%, những nguyên nhân nào sau đây là thủ phạm phổ biến? (Chọn các đáp án đúng)',
    mode: 'multi',
    options: [
      { id: 'g1', label: 'DataLoader sử dụng `num_workers=0`, khiến CPU đọc và giải nén ảnh đơn luồng quá chậm (CPU I/O Bottleneck).', isCorrect: true },
      { id: 'g2', label: 'Đọc dữ liệu trực tiếp từng file nhỏ qua kết nối mạng thay vì lưu cache trên bộ nhớ đệm hoặc định dạng streaming tối ưu (Ray Data / WebDataset).', isCorrect: true },
      { id: 'g3', label: 'Batch size quá nhỏ khiến GPU không tận dụng được toàn bộ các nhân CUDA song song.', isCorrect: true },
      { id: 'g4', label: 'Sử dụng biến môi trường `CUDA_VISIBLE_DEVICES="0,1"` trên máy chủ có 2 card GPU.', isCorrect: false },
      { id: 'g5', label: 'Ghi log metrics vào file CSV cục bộ sau mỗi 100 bước huấn luyện.', isCorrect: false },
    ],
    success: 'Chính xác! Nghẽn I/O tại CPU, mạng đọc file chậm và batch size quá nhỏ là 3 thủ phạm chính khiến GPU bị bỏ đói (Starvation).',
    error: 'Chưa chính xác. GPU utilization thấp thường do CPU đọc data chậm, batch size quá bé hoặc nghẽn mạng I/O.',
  },
  {
    id: 'cost-optimization-architecture',
    title: 'Tối ưu Chi phí Hạ tầng Doanh nghiệp',
    prompt: 'Nhóm AI có ngân sách điện toán cố định nhưng cần chạy hàng trăm thử nghiệm tinh chỉnh mô hình mỗi tuần. Giải pháp kiến trúc nào mang lại hiệu quả chi phí cao nhất?',
    mode: 'single',
    options: [
      { id: 'co1', label: 'Xây dựng cụm tính toán co giãn (Elastic Autoscaling Cluster) sử dụng GPU Spot Instances kết hợp Checkpoint Manager tự phục hồi.', isCorrect: true },
      { id: 'co2', label: 'Ký hợp đồng thuê cố định dài hạn 50 máy chủ GPU On-Demand 24/7 để đảm bảo máy luôn sẵn sàng.', isCorrect: false },
      { id: 'co3', label: 'Chuyển toàn bộ tác vụ huấn luyện Deep Learning sang chạy trên CPU để không phải trả tiền GPU.', isCorrect: false },
      { id: 'co4', label: 'Chỉ cho phép một kỹ sư được huấn luyện mô hình tại một thời điểm duy nhất trong ngày.', isCorrect: false },
    ],
    success: 'Đúng! Cụm tính toán co giãn với Spot Instances kết hợp lưu Checkpoint định kỳ giúp tiết kiệm tới 70% ngân sách mà vẫn đảm bảo an toàn tiến độ.',
    error: 'Chưa chính xác. Elastic Autoscaling trên Spot Instances là tiêu chuẩn vàng để tối ưu chi phí hạ tầng điện toán AI.',
  },
  {
    id: 'k8s-vs-ray-roles',
    title: 'Phân định Vai trò: Kubernetes vs Ray',
    prompt: 'Phân loại các nhiệm vụ kỹ thuật sau vào đúng thế mạnh cốt lõi của Kubernetes hay Ray:',
    mode: 'categorize',
    oneByOne: true,
    categories: [
      { id: 'k-k8s', label: 'Thế mạnh của Kubernetes (K8s)' },
      { id: 'k-ray', label: 'Thế mạnh của Ray' },
    ],
    options: [
      { id: 'r1', label: 'Điều phối tính toán phân tán trực tiếp bằng Python thuần qua các hàm `@ray.remote` và Actors', categoryId: 'k-ray' },
      { id: 'r2', label: 'Quản lý tài nguyên hạ tầng phần cứng, hạn ngạch mạng (Network Policies) và cô lập Container cấp hệ điều hành', categoryId: 'k-k8s' },
      { id: 'r3', label: 'Tối ưu truyền dữ liệu lớn qua bộ nhớ chia sẻ không cần tuần tự hóa (Plasma In-Memory Object Store)', categoryId: 'k-ray' },
      { id: 'r4', label: 'Tự động khởi tạo và phục hồi các Node máy chủ vật lý bị lỗi thông qua Cluster Autoscaler', categoryId: 'k-k8s' },
      { id: 'r5', label: 'Điều phối vòng đời huấn luyện phân tán (Ray Train) và tinh chỉnh siêu tham số song song (Ray Tune)', categoryId: 'k-ray' },
      { id: 'r6', label: 'Cung cấp cơ chế phân quyền bảo mật truy cập hạ tầng (RBAC) và quản lý bí mật bảo mật (Secrets Management)', categoryId: 'k-k8s' },
    ],
    success: 'Tuyệt vời! Bạn đã phân định rõ ràng: K8s quản lý container & hạ tầng phần cứng, còn Ray quản lý tính toán phân tán cấp ứng dụng Python.',
    error: 'Hãy xem lại: K8s là container orchestrator ở cấp hệ điều hành, còn Ray là distributed compute engine ở cấp mã nguồn Python.',
  },
]} />
