export const lessonMetadata = {
  domainId: 'llm-ai-engineering',
  id: 'text-embeddings-overview-quiz',
  locale: 'vi',
  title: 'Quiz',
  headings: ['Raw text', 'Thứ tự chuyển đổi', 'Shape embedding', 'Ý nghĩa của T', 'Embedding dimension'],
  keywords: ['quiz', 'raw text', 'token ID', 'embedding', 'tensor shape', 'encoder', 'decoder'],
  pageCount: 5,
}

<MdxQuiz id="text-embeddings-overview-quiz" questions={[
  {
    id: 'raw-text-needs-contract', title: 'Raw text chưa phải tensor', prompt: 'Vì sao neural network chưa thể thao tác trực tiếp trên chuỗi `"Linh builds AI."`?', mode: 'single',
    options: [
      { id: 'numeric-shape', label: 'Vì chuỗi chưa phải tensor số có dtype và shape xác định.', isCorrect: true },
      { id: 'english-only', label: 'Vì neural network không xử lý được text tiếng Anh.' },
      { id: 'four-characters', label: 'Vì mỗi token bắt buộc phải có đúng bốn ký tự.' },
      { id: 'one-scalar', label: 'Vì mọi input phải được đổi thành đúng một số duy nhất.' },
    ],
    success: 'Đúng. Text cần được chuyển thành dữ liệu số có dtype và shape xác định trước khi đi vào các phép toán của model.',
    error: 'Vấn đề nằm ở dạng dữ liệu: raw string chưa phải tensor số có dtype và shape xác định.',
  },
  {
    id: 'raw-to-embedding-order', title: 'Thứ tự chuyển đổi', prompt: 'Thứ tự nào khớp với flow chi tiết trên trang?', mode: 'single',
    options: [
      { id: 'raw-id-embedding', label: 'Raw text → Token IDs → Embedding tensor.', isCorrect: true },
      { id: 'embedding-id-raw', label: 'Embedding tensor → Token IDs → Raw text.' },
      { id: 'raw-embedding-id', label: 'Raw text → Embedding tensor → Token IDs.' },
      { id: 'id-raw-embedding', label: 'Token IDs → Raw text → Embedding tensor.' },
    ],
    success: 'Đúng. Tokenizer tạo ID trước; sau đó mỗi ID được thay bằng một vector embedding.',
    error: 'Hãy đọc ba block theo chiều mũi tên từ trái sang phải.',
  },
  {
    id: 'example-embedding-shape', title: 'Shape của ví dụ', prompt: 'Ví dụ có 4 token và mỗi token được thay bằng một vector gồm 3 số. Embedding tensor có shape nào?', mode: 'single',
    options: [
      { id: 'four-three', label: '`(4, 3)`', isCorrect: true },
      { id: 'three-four', label: '`(3, 4)`' },
      { id: 'four-one', label: '`(4, 1)`' },
      { id: 'one-three', label: '`(1, 3)`' },
    ],
    success: 'Đúng. Có `T=4` hàng, mỗi hàng chứa `C=3` số, nên shape là `(4, 3)`.',
    error: 'Shape được viết theo thứ tự `(T, C)`: số token trước, số phần tử trong mỗi vector sau.',
  },
  {
    id: 'sequence-axis-meaning', title: 'Ý nghĩa của T', prompt: 'Trong shape `(T, C)`, `T` biểu diễn điều gì?', mode: 'single',
    options: [
      { id: 'token-count', label: 'Số token trong input.', isCorrect: true },
      { id: 'word-count', label: 'Số từ trong input, luôn bằng số token.' },
      { id: 'feature-count', label: 'Số phần tử trong vector của mỗi token.' },
      { id: 'id-value', label: 'Giá trị lớn nhất trong danh sách token ID.' },
    ],
    success: 'Đúng. `T` là số token trong input; nó không nhất thiết bằng số từ.',
    error: '`T` đếm số token. Trong ví dụ, dấu chấm cũng là một token nên `T=4`.',
  },
  {
    id: 'embedding-dimension-choice', title: 'Embedding dimension', prompt: 'Trong thực tế, con số `C=3` trong ví dụ có thể được thay bằng giá trị nào?', mode: 'single',
    options: [
      { id: 'practical-dimensions', label: '`128`, `256` hoặc `512`, tùy thiết kế model.', isCorrect: true },
      { id: 'token-count-only', label: 'Chỉ có thể bằng số token trong input.' },
      { id: 'largest-id-only', label: 'Chỉ có thể bằng token ID lớn nhất.' },
      { id: 'always-three', label: 'Luôn phải bằng `3` ở mọi model.' },
    ],
    success: 'Đúng. `C=3` chỉ dùng để minh họa; model thực tế có thể chọn `128`, `256`, `512` hoặc giá trị khác tùy thiết kế.',
    error: '`C=3` chỉ là lựa chọn minh họa. Model thực tế thường dùng dimension lớn hơn và giữ `C` cố định trong cùng model.',
  },
]} />
