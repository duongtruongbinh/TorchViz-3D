export const lessonMetadata = {
  domainId: 'llm-ai-engineering',
  id: 'tokenizer-regex-from-scratch-quiz',
  locale: 'vi',
  title: 'Quiz',
  headings: ['Capturing group', 'Kết quả split dấu câu', 'Làm sạch token', 'Hàm tokenize', 'Giới hạn của regex'],
  keywords: ['Python', 'regex', 're.split', 'capturing group', 'whitespace', 'punctuation', 'list comprehension', 'tokenizer'],
  pageCount: 5,
}

<MdxQuiz id="tokenizer-regex-from-scratch-quiz" questions={[
  {
    id: 'capturing-whitespace', title: 'Split theo whitespace', prompt: 'Trong `re.split(r"(\\s)", text)`, cặp ngoặc quanh `\\s` làm thay đổi output như thế nào?', mode: 'single',
    options: [
      { id: 'keep-separator', label: 'Whitespace khớp pattern được giữ lại như các phần tử riêng trong danh sách.', isCorrect: true },
      { id: 'remove-all-space', label: 'Mọi whitespace bị xoá hoàn toàn khỏi output.' },
      { id: 'split-punctuation', label: 'Dấu phẩy, dấu chấm và dấu hai chấm tự động trở thành token riêng.' },
      { id: 'merge-words', label: 'Các từ ở hai phía whitespace được ghép thành một token.' },
    ],
    success: 'Đúng. Capturing group khiến `re.split` trả cả separator; vì vậy output vẫn chứa các phần tử whitespace giữa các từ.',
    error: 'Không có bước cleanup ở dòng này. Ngoặc tạo capturing group, nên phần whitespace khớp pattern vẫn xuất hiện trong kết quả.',
  },
  {
    id: 'punctuation-split-output', title: 'Đọc kết quả regex', prompt: 'Sau `re.split(r"([,.]|\\s)", text)`, những nhận xét nào đúng?', mode: 'multi',
    options: [
      { id: 'all-punctuation', label: 'Mọi loại dấu câu, gồm `:`, `!`, dấu ngoặc kép và `--`, đều đã được tách riêng.' },
      { id: 'comma-period', label: 'Dấu phẩy và dấu chấm trở thành phần tử riêng.', isCorrect: true },
      { id: 'no-empty', label: 'Danh sách không thể chứa chuỗi rỗng vì regex đã tự làm sạch output.' },
      { id: 'empty-items', label: 'Chuỗi rỗng có thể xuất hiện cạnh separator và cần được lọc ở bước sau.', isCorrect: true },
    ],
    success: 'Đúng. Pattern mới chỉ biết dấu phẩy, dấu chấm và whitespace; capturing split cũng có thể sinh chuỗi rỗng.',
    error: 'Hãy đọc đúng phạm vi character class `[,.]`: nó chưa chứa `:`, `!` hoặc dấu ngoặc kép. Output minh hoạ cũng cho thấy các phần tử rỗng.',
  },
  {
    id: 'cleanup-comprehension', title: 'Làm sạch kết quả', prompt: 'Dòng `[item.strip() for item in result if item.strip()]` thực hiện điều gì?', mode: 'single',
    options: [
      { id: 'sort-tokens', label: 'Sắp xếp token theo alphabet rồi loại token trùng.' },
      { id: 'merge-punctuation', label: 'Ghép mọi dấu câu trở lại từ đứng trước.' },
      { id: 'assign-ids', label: 'Gán một số nguyên duy nhất cho mỗi token.' },
      { id: 'strip-filter', label: 'Bỏ whitespace quanh phần tử và chỉ giữ phần tử còn nội dung.', isCorrect: true },
    ],
    success: 'Đúng. `item.strip()` ở phần điều kiện lọc chuỗi rỗng/whitespace; biểu thức đầu danh sách trả về nội dung đã strip.',
    error: 'List comprehension này chưa sắp xếp, merge hay gán ID. Nó chỉ chuẩn hoá whitespace và lọc phần tử không còn nội dung.',
  },
  {
    id: 'tokenize-function', title: 'Đóng gói pipeline', prompt: 'Luồng xử lý nào mô tả đúng hàm `tokenize(text)` trong bài?', mode: 'single',
    options: [
      { id: 'clean-before-split', label: 'Xoá toàn bộ dấu câu trước, sau đó split từng ký tự.' },
      { id: 'split-clean-return', label: 'Split bằng regex → strip và lọc phần tử rỗng → trả về danh sách token.', isCorrect: true },
      { id: 'build-vocabulary', label: 'Đếm corpus → học merge → tạo vocabulary → trả về token ID.' },
      { id: 'embed-tokens', label: 'Split text → tra embedding vector → trả về tensor.' },
    ],
    success: 'Đúng. Hàm chỉ đóng gói hai bước đã học để cùng một pipeline có thể tái sử dụng cho câu ví dụ và `raw_text`.',
    error: 'Hàm trong bài chưa huấn luyện vocabulary, gán ID hoặc tạo embedding. Nó chỉ split, cleanup và trả token string.',
  },
  {
    id: 'regex-limitations', title: 'Từ regex đến BPE', prompt: 'Vì sao tokenizer regex này chưa đủ cho một LLM thực tế?', mode: 'multi',
    options: [
      { id: 'no-corpus-learning', label: 'Các quy tắc không tự học chuỗi con phổ biến hoặc merge từ corpus.', isCorrect: true },
      { id: 'already-optimal', label: 'Regex đã tự tạo vocabulary tối ưu nên không cần bước huấn luyện tokenizer.' },
      { id: 'coverage-hard', label: 'Bộ quy tắc viết tay khó bao phủ ổn định mọi ngôn ngữ, ký tự và cách viết.', isCorrect: true },
      { id: 'attention-required', label: 'Tokenizer chỉ hoạt động sau khi Transformer đã chạy attention.' },
    ],
    success: 'Đúng. Regex giúp thấy rõ cơ chế tách chuỗi, nhưng không học vocabulary từ dữ liệu và rất khó bao phủ các trường hợp đa dạng. Đó là cầu nối sang BPE.',
    error: 'Hãy phân biệt tokenizer viết tay với tokenizer được huấn luyện: regex không tự học merge/vocabulary và độ bao phủ phụ thuộc hoàn toàn vào pattern do ta viết.',
  },
]} />
