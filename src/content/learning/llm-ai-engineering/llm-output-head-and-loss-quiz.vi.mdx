export const lessonMetadata = {
  domainId: 'llm-ai-engineering',
  id: 'llm-output-head-and-loss-quiz', locale: 'vi', title: 'Quiz',
  headings: ['Context vector', 'Output projection', 'Logits', 'Softmax'],
  keywords: ['context vector', 'linear layer', 'logits', 'softmax'], pageCount: 4,
}

<MdxQuiz id="llm-output-head-and-loss-checkpoint-quiz" questions={[
  {
    id: 'context-vector-role', title: 'Context vector', prompt: 'Vector nào được dùng để dự đoán token tiếp theo trong pipeline của bài?', mode: 'single',
    options: [
      { id: 'mean-raw', label: 'Trung bình của các raw input embedding trước khi đi qua neural network.' },
      { id: 'last-raw', label: 'Raw embedding của token cuối cùng, chưa nhận thông tin từ context.' },
      { id: 'last-contextualized', label: 'Vector tại vị trí token cuối cùng sau khi đã nhận thông tin từ các token trước.', isCorrect: true },
      { id: 'first-contextualized', label: 'Vector tại vị trí token đầu tiên vì đây là điểm bắt đầu của câu.' },
    ],
    success: 'Đúng. Biểu diễn ở vị trí cuối đã nhận thông tin từ các token trước và được dùng làm h.',
    error: 'Chưa đúng. Hãy xem lại bước tạo context vector h.',
  },
  {
    id: 'projection-shape', title: 'Shape của output head', prompt: 'Context vector có kích thước d. Linear output head cần tạo ra vector có kích thước nào?', mode: 'single',
    options: [
      { id: 'd', label: 'd, vì linear layer chỉ thay đổi giá trị chứ không đổi số chiều.' },
      { id: 'vocab', label: '|V|, để tạo một logit cho mỗi token trong vocabulary.', isCorrect: true },
      { id: 'context-concat', label: 'd × số token trong context, vì mọi vector context được nối lại.' },
      { id: 'layers', label: 'Bằng số layer của neural network, vì mỗi layer tạo một output score.' },
    ],
    success: 'Đúng. Output head chiếu context vector thành một logit cho mỗi token trong vocabulary.',
    error: 'Chưa đúng. Hãy nhớ logits cần có một vị trí cho mọi token trong vocabulary.',
  },
  {
    id: 'logits-properties', title: 'Logits', prompt: 'Phát biểu nào đúng về vector logits z?', mode: 'single',
    options: [
      { id: 'probabilities', label: 'Có |V| giá trị không âm và tổng đã bằng 1.' },
      { id: 'rankings', label: 'Có |V| thứ hạng; mỗi phần tử chỉ biểu thị thứ tự ưu tiên của token.' },
      { id: 'dimension-d', label: 'Có d score thực, sau đó softmax tự mở rộng thành |V| phần tử.' },
      { id: 'real-scores', label: 'Có |V| score chưa chuẩn hóa; mỗi score có thể âm, bằng 0 hoặc dương.', isCorrect: true },
    ],
    success: 'Đúng. Vector z có |V| score thực chưa chuẩn hóa, mỗi score ứng với một token.',
    error: 'Chưa đúng. Logits xuất hiện trước softmax nên chưa phải xác suất.',
  },
  {
    id: 'softmax-role', title: 'Softmax', prompt: 'Softmax làm gì ở output head?', mode: 'single',
    options: [
      { id: 'normalize', label: 'Chuyển |V| logits thành |V| xác suất có tổng bằng 1.', isCorrect: true },
      { id: 'argmax', label: 'Chọn token có xác suất lớn nhất và loại bỏ phần còn lại của vector.' },
      { id: 'learn-projection', label: 'Học ma trận W dùng để chiếu h từ d sang |V|.' },
      { id: 'sort', label: 'Sắp xếp logits theo thứ tự giảm dần mà không đổi giá trị.' },
    ],
    success: 'Đúng. Softmax chuẩn hóa logits thành phân phối xác suất.',
    error: 'Chưa đúng. Linear layer tạo logits; softmax mới chuẩn hóa chúng.',
  },
]} />
