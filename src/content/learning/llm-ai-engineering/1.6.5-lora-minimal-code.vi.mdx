export const lessonMetadata = {
  domainId: 'llm-ai-engineering',
  id: 'lora-minimal-code',
  locale: 'vi',
  title: 'Code LoRA tối thiểu',
  headings: ['Contract', 'Adapter', 'Wrapper', 'Freeze', 'Smoke test'],
  keywords: ['PyTorch', 'LoRA code', 'LinearWithLoRA', 'frozen weights', 'trainable parameters'],
  pageCount: 5,
}

<MdxPage page={0}>

<CodeLessonFrame
  title="Contract của LoRA linear"
  contract="(...,d_in) → base + adapter → (...,d_out)"
  output="Cùng output ban đầu; chỉ A/B trainable"
  pitfall="Implementation giáo dục chưa quản lý target modules, quantization hoặc merge/unmerge."
  takeaway="LoRA là một nhánh cộng thêm, không thay contract của linear."
>

**Mục tiêu:** Bọc một `nn.Linear(d_in,d_out)` sao cho base weight bị đóng băng, output shape không đổi và chỉ A/B trainable.

Input có shape `(...,d_in)`; cả base branch và LoRA branch trả `(...,d_out)`. Adapter ban đầu phải tạo update 0 để wrapper khớp layer gốc trước training.

</CodeLessonFrame>

</MdxPage>

<MdxPage page={1}>

<CodeLessonFrame
  title="Hai ma trận hạng thấp"
  contract="d_in → rank → d_out"
  output="B=0 làm adapter update ban đầu bằng 0"
  pitfall="Khởi tạo cả A và B bằng 0 có thể chặn đường gradient hữu ích ban đầu."
  takeaway="Shape của adapter đi d_in → rank → d_out."
>

```python
import torch
from torch import nn

class LoRAAdapter(nn.Module):
    def __init__(self, d_in, d_out, rank=4, alpha=8.0):
        super().__init__()
        if d_in <= 0 or d_out <= 0:
            raise ValueError("d_in and d_out must be positive")
        if not isinstance(rank, int) or not 0 < rank <= min(d_in, d_out):
            raise ValueError("rank must be an integer in [1, min(d_in,d_out)]")
        self.rank = rank
        self.alpha = alpha
        self.A = nn.Parameter(torch.empty(rank, d_in))
        self.B = nn.Parameter(torch.zeros(d_out, rank))
        nn.init.kaiming_uniform_(self.A, a=5 ** 0.5)

    def forward(self, x):
        return (x @ self.A.T @ self.B.T) * (
            self.alpha / self.rank
        )
```

`B=0` làm update ban đầu bằng 0.

</CodeLessonFrame>

</MdxPage>

<MdxPage page={2}>

<CodeLessonFrame
  title="Bọc linear pretrained"
  contract="existing nn.Linear + LoRAAdapter → same output shape"
  output="base(x) + lora(x)"
  pitfall="Không tạo linear mới rồi làm mất pretrained weights."
  takeaway="Base branch bất biến; adapter branch học task-specific update."
>

```python
class LinearWithLoRA(nn.Module):
    def __init__(self, linear, rank=4, alpha=8.0):
        super().__init__()
        self.linear = linear
        self.lora = LoRAAdapter(
            linear.in_features,
            linear.out_features,
            rank,
            alpha,
        )
        for parameter in self.linear.parameters():
            parameter.requires_grad = False

    def forward(self, x):
        return self.linear(x) + self.lora(x)
```

</CodeLessonFrame>

</MdxPage>

<MdxPage page={3}>

<CodeLessonFrame
  title="Kiểm tra parameter trainable"
  contract="requires_grad audit → count A/B only"
  output="rank 2, 16→12: 2×(16+12)=56"
  takeaway="Công thức r(d_in+d_out) phải khớp numel() trong code."
>

```python
def count_trainable(module):
    return sum(
        p.numel() for p in module.parameters()
        if p.requires_grad
    )

base = nn.Linear(16, 12, bias=False)
layer = LinearWithLoRA(base, rank=2, alpha=4)

assert not layer.linear.weight.requires_grad
assert layer.lora.A.requires_grad
assert layer.lora.B.requires_grad
assert count_trainable(layer) == 2 * (16 + 12)
```

<LessonNote>**Production:** optimizer chỉ nên nhận parameter có `requires_grad=True`; log cả tổng và trainable count trước training.</LessonNote>

</CodeLessonFrame>

</MdxPage>

<MdxPage page={4}>

<CodeLessonFrame
  title="Smoke test độc lập"
  contract="same initial output + correct shape + frozen base + rank validation"
  output="count_trainable(wrapped) = 14"
  takeaway="Smoke test khóa các invariant trước khi dùng adapter trong training."
>

```python
import torch
from torch import nn

torch.manual_seed(3)
base = nn.Linear(4, 3, bias=False)
wrapped = LinearWithLoRA(base, rank=2, alpha=2)
x = torch.randn(5, 4)

with torch.no_grad():
    original = base(x)
    initial = wrapped(x)

assert initial.shape == (5, 3)
assert torch.allclose(initial, original)

loss = wrapped(x).square().mean()
loss.backward()
assert wrapped.linear.weight.grad is None
assert wrapped.lora.B.grad is not None

try:
    LoRAAdapter(4, 3, rank=0)
except ValueError:
    pass
else:
    raise AssertionError("rank=0 must fail")

try:
    LoRAAdapter(4, 3, rank=4)
except ValueError:
    pass
else:
    raise AssertionError("rank above min(d_in,d_out) must fail")

print(count_trainable(wrapped))  # 14
```

Sau một optimizer step, adapter output có thể khác 0 trong khi base weight vẫn giữ nguyên.

</CodeLessonFrame>

</MdxPage>
