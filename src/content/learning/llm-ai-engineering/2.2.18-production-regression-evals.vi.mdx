export const lessonMetadata = {
  domainId: 'llm-ai-engineering',
  id: 'production-regression-evals',
  locale: 'vi',
  title: 'Regression evaluation trong production',
  headings: ['Từ failure thành invariant', 'Versioned run contract', 'Worked release gate', 'Pitfall và handoff'],
  keywords: ['regression evaluation', 'release gate', 'versioning', 'monitoring', 'CI'],
  pageCount: 4,
}

<MdxPage page={0}>

<EvaluationConceptVisual content={{
  view: 'audit',
  lead: {vi: 'Một production failure chỉ hoàn tất khi trở thành invariant được replay và monitoring.'},
  steps: [
    {label: {vi: 'Observed'}, shape: 'stable ID + sanitized input', detail: {vi: 'Gắn severity và owner.'}},
    {label: {vi: 'Reproduced → fixed'}, shape: 'expected invariant', detail: {vi: 'Xác nhận root behavior và mitigation.'}},
    {label: {vi: 'Regression case'}, shape: 'versioned suite', detail: {vi: 'Tách smoke, core task và safety/authorization.'}},
    {label: {vi: 'Monitored'}, shape: 'scheduled deep eval', detail: {vi: 'Critical invariant giữ gate riêng.'}},
  ],
  code: [],
  takeaway: {vi: 'Một demo pass không đủ để đóng incident.'},
}} />

</MdxPage>

<MdxPage page={1}>

<EvaluationConceptVisual content={{
  view: 'audit',
  lead: {vi: 'Versioned run contract khóa mọi behavioral dependency trước khi so kết quả.'},
  steps: [
    {label: {vi: 'Data + system'}, shape: 'dataset hash + model snapshot + prompt', detail: {vi: 'Provider alias phải có declared version/date.'}},
    {label: {vi: 'Runtime dependencies'}, shape: 'retrieval + tools + policy', detail: {vi: 'Ghi đúng cấu hình được evaluate.'}},
    {label: {vi: 'Measurement'}, shape: 'evaluator + thresholds + code revision', detail: {vi: 'Thước đo cũng được version.'}},
    {label: {vi: 'Outputs'}, shape: 'per-case + slices + artifacts + decision', detail: {vi: 'pass/review/block với request IDs, không raw secret/PII.'}},
  ],
  code: [],
  takeaway: {vi: 'Run khác evaluator/protocol không phải cùng một thước đo.'},
}} />

</MdxPage>

<MdxPage page={2}>

<EvaluationConceptVisual content={{
  view: 'position',
  lead: {vi: 'Worked release gate: task improvement không được ghi đè authorization regression.'},
  comparisons: [
    {title: 'Baseline', shape: 'task 90/100 · auth 20/20', detail: {vi: 'Đạt critical authorization suite.'}},
    {title: 'Candidate', shape: 'task 92/100 · auth 19/20', detail: {vi: 'Tăng 2 điểm task nhưng fail một must-pass case.'}},
    {title: 'Decision', shape: 'block', detail: {vi: 'In case ID, invariant và owner—not chỉ score below threshold.'}},
  ],
  code: [],
  takeaway: {vi: 'Nếu evaluator đổi, chạy lại baseline và candidate bằng cùng version trước khi so.'},
}} />

</MdxPage>

<MdxPage page={3}>

<EvaluationConceptVisual content={{
  view: 'audit',
  lead: {vi: 'Regression handoff nối online failures với offline gate mà vẫn bảo vệ dữ liệu production.'},
  steps: [
    {label: {vi: 'Sanitize traffic'}, shape: 'redaction + consent', detail: {vi: 'Không dùng raw production data trực tiếp.'}},
    {label: {vi: 'Comparable runs'}, shape: 'same evaluator/protocol', detail: {vi: 'Giữ measurement contract.'}},
    {label: {vi: 'Report vector'}, shape: 'slices + uncertainty + latency/cost + critical', detail: {vi: 'Không chỉ một aggregate.'}},
    {label: {vi: 'Close loop'}, shape: 'online failure → offline suite → gate', detail: {vi: 'Gate trước exposure tiếp theo.'}},
  ],
  code: [],
  takeaway: {vi: 'A/B lesson kế tiếp đo effect cùng guardrail trước rollout.'},
}} />

</MdxPage>
