export const lessonMetadata = {
  domainId: 'llm-ai-engineering',
  id: 'assistant-turn-history',
  locale: 'vi',
  title: 'Assistant history và state nhiều lượt',
  headings: ['History không phải database', 'Tính budget nhiều lượt', 'Production gate'],
  keywords: ['assistant history', 'conversation state', 'token budget', 'authority'],
  pageCount: 3,
}

<MdxPage page={0}>

<PromptConceptVisual content={{
  view: 'audit',
  lead: {vi: 'History giữ continuity nhưng không phải database hay nguồn authority cho business state.'},
  steps: [
    {label: {vi: 'Accepted turns'}, shape: 'server-verified user/assistant order', detail: {vi: 'Client không được giả assistant role.'}},
    {label: {vi: 'Tool results'}, shape: 'redacted + TTL', detail: {vi: 'Không mang dữ liệu nhạy cảm vô hạn.'}},
    {label: {vi: 'Current state'}, shape: 'authoritative backend source', detail: {vi: 'Transcript order status chỉ là snapshot.'}},
    {label: {vi: 'Minimal history'}, shape: 'ordered + within budget', detail: {vi: 'Chỉ giữ context cần thiết cho lượt mới.'}},
  ],
  code: [],
  takeaway: {vi: 'Tải lại business state trước action; output cũ và lỗi cũ không tự trở thành sự thật.'},
}} />

</MdxPage>

<MdxPage page={1}>

<CodeLessonFrame
  title="Tính budget nhiều lượt"
  contract="turn tokens + system + output reserve ≤ context capacity"
  output="used 1,360 · remaining 688 trong capacity 2,048"
  pitfall="Sau truncate/summarize phải đếm lại bằng đúng tokenizer revision."
  takeaway="Reserve output trước rồi mới phân bổ history và context mới."
>

Ba turn dùng 180, 220 và 260 token; system dùng 300 và output reserve 400. Tổng đã cam kết là `1.360` token. Với capacity 2.048, còn 688 token cho context mới.

~~~ts
import assert from "node:assert/strict";

type HistoryBudget = { used: number; remaining: number };

function historyBudget(
  turnTokens: readonly number[],
  systemTokens: number,
  outputReserve: number,
  capacity: number,
): HistoryBudget {
  const values = [...turnTokens, systemTokens, outputReserve, capacity];
  if (values.some((value) => !Number.isInteger(value) || value < 0) || capacity === 0) {
    throw new Error("invalid_token_budget");
  }
  const used = turnTokens.reduce((sum, value) => sum + value, 0) + systemTokens + outputReserve;
  if (used > capacity) throw new Error("context_budget_exceeded");
  return { used, remaining: capacity - used };
}

const budget = historyBudget([180, 220, 260], 300, 400, 2_048);
assert.deepEqual(budget, { used: 1_360, remaining: 688 });
assert.throws(() => historyBudget([900], 300, 400, 1_000), /context_budget_exceeded/);
assert.throws(() => historyBudget([180, -1], 300, 400, 2_048), /invalid_token_budget/);
~~~

<LessonNote>Token estimate phải dùng đúng tokenizer revision của model. Sau khi cắt/summarize history, chạy lại counter thay vì tin phép ước lượng cũ.</LessonNote>

</CodeLessonFrame>

</MdxPage>

<MdxPage page={2}>

<PromptConceptVisual content={{
  view: 'audit',
  lead: {vi: 'Production gate bảo vệ role, authority, budget và privacy của multi-turn state.'},
  steps: [
    {label: {vi: 'Verify turns'}, shape: 'server role/order', detail: {vi: 'Không tin client messages array.'}},
    {label: {vi: 'Expire data'}, shape: 'TTL + redaction', detail: {vi: 'Summary không trở thành business source of truth.'}},
    {label: {vi: 'Budget'}, shape: 'reserve → allocate → truncate', detail: {vi: 'Đếm lại sau mọi thay đổi.'}},
    {label: {vi: 'Minimal logs'}, shape: 'counts + versions + errors', detail: {vi: 'Không raw conversation, PII, secret hay hidden reasoning.'}},
  ],
  code: [],
  takeaway: {vi: 'Quiz tiếp theo kiểm tra role, authority và state.'},
}} />

</MdxPage>
