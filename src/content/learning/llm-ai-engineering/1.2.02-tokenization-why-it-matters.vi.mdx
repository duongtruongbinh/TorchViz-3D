export const lessonMetadata = {
  domainId: 'llm-ai-engineering',
  id: 'tokenization-why-it-matters',
  locale: 'vi',
  title: 'Tại sao dùng token thay vì chỉ dùng word?',
  headings: ['Token là đơn vị của tokenizer', 'Ba mức chia text', 'Vocabulary và sequence length', 'Tokenizer là model contract'],
  keywords: ['tokenizer', 'token', 'word-level', 'character-level', 'subword', 'vocabulary', 'sequence length'],
  pageCount: 4,
}

<MdxPage page={0}>

## Token là đơn vị do tokenizer quyết định

**Input:** một chuỗi raw text.  
**Output:** một danh sách token có thứ tự.

Một token có thể là cả từ, mảnh từ, dấu câu, khoảng trắng đi kèm hoặc một đơn vị byte. Token không đồng nghĩa với “word”.

<TokenizerMemory content={{
  cards: [
    { id: 'flexible', cue: { vi: 'Đơn vị' }, title: { vi: 'Token không chỉ là word' }, example: 'Linh · builds · AI · .', description: { vi: 'Tokenizer có thể giữ từ phổ biến thành một token và tách dấu câu thành token riêng.' } },
    { id: 'robust', cue: { vi: 'Bao phủ' }, title: { vi: 'Đọc được cách viết mới' }, example: 'build · er · s', description: { vi: 'Subword hoặc byte fallback giúp biểu diễn chuỗi chưa từng xuất hiện nguyên vẹn trong vocabulary.' } },
    { id: 'efficient', cue: { vi: 'Cân bằng' }, title: { vi: 'Không chia quá thô hoặc quá vụn' }, example: 'word ↔ subword ↔ char', description: { vi: 'Đơn vị lớn làm vocabulary phình ra; đơn vị quá nhỏ làm sequence dài.' } },
  ],
}} />

</MdxPage>

<MdxPage page={1}>

<TokenizerGranularity content={{
  source: 'builders',
  approaches: [
    {id: 'word', title: 'Word-level', tokens: ['builders'], strength: {vi: 'Tạo sequence ngắn khi từ đã có trong vocabulary.'}, cost: {vi: 'Từ mới dễ nằm ngoài vocabulary.'}},
    {id: 'character', title: 'Character-level', tokens: ['b', 'u', 'i', 'l', 'd', 'e', 'r', 's'], strength: {vi: 'Bao phủ được cách viết mới bằng đơn vị nhỏ.'}, cost: {vi: 'Sequence dài hơn nhiều.'}},
    {id: 'subword', title: 'Subword', tokens: ['build', 'ers'], strength: {vi: 'Tái sử dụng mảnh phổ biến và giữ sequence vừa phải.'}, cost: {vi: 'Ranh giới phụ thuộc vocabulary và merge rules.'}},
  ],
  whitespaceNote: {vi: 'Dấu câu và whitespace cũng cần quy tắc. Trong “ AI.”, tokenizer production có thể gắn khoảng trắng vào token “ AI” thay vì xóa nó.'},
  misconception: {vi: 'Không mặc định dùng text.split(): cách đó làm mất thông tin whitespace và để dấu câu dính vào từ.'},
}} />

**Handoff:** Bài regex tiếp theo sẽ cho thấy rõ từng quyết định split và cleanup.

</MdxPage>

<MdxPage page={2}>

## Vocabulary size và sequence length đổi chỗ cho nhau

<TokenizerSequenceLength content={{
  characterTokens: ['b', 'u', 'i', 'l', 'd', 'e', 'r', 's', '␠', 'A', 'I'],
  subwordTokens: ['build', 'ers', '␠', 'AI'],
  characterCount: '11 tokens',
  subwordCount: '4 tokens',
  takeaway: { vi: 'Character-level cần vocabulary nền tảng nhỏ nhưng tạo nhiều vị trí hơn. Subword thêm các chuỗi thường gặp vào vocabulary để rút ngắn sequence. Không có lựa chọn luôn tốt nhất cho mọi corpus.' },
}} />

<LessonNote>**Lỗi thường gặp:** vocabulary càng lớn luôn càng tốt. Bảng embedding/output sẽ lớn hơn, trong khi các token hiếm có thể được học kém.</LessonNote>

**Handoff:** BPE sẽ học điểm cân bằng này từ thống kê corpus thay vì viết tay mọi token.

</MdxPage>

<MdxPage page={3}>

<TokenizerContract content={{
  lead: {vi: 'Tokenizer không chỉ cắt text. Nó khóa bốn quyết định mà checkpoint và data pipeline phải dùng thống nhất.'},
  decisions: [
    {id: 'vocabulary', title: {vi: 'Vocabulary size'}, value: 'V', meaning: {vi: 'Số token có thể tra trong embedding và output table.'}},
    {id: 'ids', title: {vi: 'Token IDs'}, value: 'token ↔ ID', meaning: {vi: 'Mapping ổn định cho token thường và special token.'}},
    {id: 'length', title: {vi: 'Sequence length'}, value: 'T', meaning: {vi: 'Số vị trí sinh ra cho mỗi input sau encode.'}},
    {id: 'roundtrip', title: {vi: 'Encode / decode'}, value: 'text ↔ IDs', meaning: {vi: 'Quy tắc biến đổi hai chiều theo đúng normalization.'}},
  ],
  checkpoint: {vi: 'Hai tokenizer có thể tạo hai dãy ID khác nhau cho cùng text; token IDs chỉ hợp lệ với đúng vocabulary và checkpoint đi kèm.'},
  misconception: {vi: 'Không thay tokenizer nhưng giữ nguyên model weights: ID 2 có thể trỏ đến token khác và làm model tra sai hàng embedding ngay từ đầu.'},
}} />

**Handoff:** Trước khi dùng tokenizer production, ta xây một tokenizer regex nhỏ để nhìn thấy contract raw text → tokens.

</MdxPage>
