export const lessonMetadata = {
  domainId: 'llm-ai-engineering',
  id: 'llm-data-pipeline-checkpoint-quiz', locale: 'vi', title: 'Quiz',
  headings: ['Corpus split', 'Window batch', 'GPT logits', 'Training update', 'Generation loop'],
  keywords: ['pretraining', 'decoder-only GPT', 'sliding window', 'next-token loss', 'generation'], pageCount: 5,
}

<MdxQuiz id="llm-data-pipeline-checkpoint-quiz" questions={[
  {
    id: 'pretraining-facts', title: 'Corpus split', prompt: 'Vì sao nên tách raw documents thành train/validation trước khi tạo sliding windows?', mode: 'single',
    options: [
      { id: 'avoid-window-leakage', label: 'Để các windows overlap gần giống nhau không rơi vào hai split', isCorrect: true },
      { id: 'smaller-vocab', label: 'Để validation dùng vocabulary nhỏ hơn train' },
      { id: 'remove-target', label: 'Để validation không cần shifted targets' },
      { id: 'enable-attention', label: 'Để causal attention hoạt động' },
    ], success: 'Đúng. Split document trước ngăn các đoạn overlap rò qua validation.', error: 'Vấn đề nằm ở leakage giữa các mẫu gần trùng, không ở vocabulary hay causal mask.',
  },
  {
    id: 'finetuning-facts', title: 'Window và batch', prompt: 'Mô tả nào đúng về sample và batch?', mode: 'single',
    options: [
      { id: 'dataset-loader-contract', label: 'Dataset tạo từng cặp `(T),(T)`; DataLoader gom thành `(B,T),(B,T)`', isCorrect: true },
      { id: 'window-is-batch', label: 'Mỗi sliding window đã là một batch hoàn chỉnh' },
      { id: 'loader-shifts', label: 'Dataset trả text; DataLoader tự tạo shifted targets' },
      { id: 'batch-vocab', label: 'Batch size quyết định vocabulary size' },
    ], success: 'Đúng. Window là sample; batching là trách nhiệm riêng của DataLoader.', error: 'Theo dõi nơi shift được tạo và nơi batch dimension được thêm.',
  },
  {
    id: 'training-stage-task-match', title: 'GPT logits', prompt: 'Input IDs `(B,T)` qua GPT với vocabulary V tạo output nào?', mode: 'single',
    options: [
      { id: 'btv-logits', label: 'Raw logits `(B,T,V)`; mọi train position có next-token target', isCorrect: true },
      { id: 'btoken', label: 'Một token ID `(B,)` cho cả sequence' },
      { id: 'btc-final', label: 'Hidden `(B,T,C)` là output cuối đã chuẩn hóa thành xác suất' },
      { id: 'loss-only', label: 'Một scalar loss dù chưa truyền targets' },
    ], success: 'Đúng. Output head thay chiều C bằng V tại mọi vị trí.', error: 'Phân biệt hidden states, vocabulary logits và loss cần targets.',
  },
  {
    id: 'transformer-main-blocks', title: 'Training update', prompt: 'Thứ tự nào đúng cho một optimizer update?', mode: 'single',
    options: [
      { id: 'correct-update', label: 'zero_grad → forward → loss → backward → optimizer.step', isCorrect: true },
      { id: 'step-first', label: 'optimizer.step → forward → backward → zero_grad' },
      { id: 'argmax-backward', label: 'forward → argmax → backward trên token ID' },
      { id: 'validation-update', label: 'model.eval → no_grad → optimizer.step' },
    ], success: 'Đúng. Gradient được tạo sau loss và dùng ở optimizer.step.', error: 'Optimizer cần gradient từ backward; zero_grad phải xử lý accumulation có chủ đích.',
  },
  {
    id: 'encoder-input-prep-order', title: 'Generation loop', prompt: 'Sau khi chọn token mới từ last-position logits, điều gì xảy ra?', mode: 'single',
    options: [
      { id: 'generation-loss-only', label: 'Token mới chỉ được dùng để tính loss rồi bị bỏ khỏi câu đích.' },
      { id: 'append', label: 'Token mới được ghép vào câu đích để làm prefix cho bước tiếp theo hoặc hoàn thành bản dịch.', isCorrect: true },
      { id: 'regenerate-prefix', label: 'Decoder xóa prefix và sinh lại toàn bộ câu đích từ đầu.' },
      { id: 'replace-source', label: 'Token mới được đưa ngược vào encoder để thay một token trong câu nguồn.' },
    ], success: 'Đúng. Autoregressive generation nối token rồi chạy vòng tiếp theo.', error: 'Token mới trở thành một phần của prefix; nó không bị bỏ hay thay input cũ.',
  },
]} />
