export const lessonMetadata = {
  domainId: 'llm-ai-engineering',
  id: 'ar-language-model-inference-pipeline-quiz', locale: 'vi', title: 'Quiz',
  headings: ['Inference order', 'Sampling', 'Corpus và vocabulary', 'Output vector', 'Logits'],
  keywords: ['tokenize', 'forward', 'logits', 'probability', 'sample', 'detokenize', 'corpus', 'vocabulary'], pageCount: 5,
}

<MdxQuiz id="ar-language-model-inference-pipeline-checkpoint-quiz" questions={[
  {
    id: 'ar-inference-order', title: 'Pipeline sinh token', prompt: 'Một implementation đang sample trực tiếp từ raw logits rồi mới gọi softmax. Sửa thứ tự nào tạo đúng một token mới?', mode: 'single',
    options: [
      { id: 'sample', label: 'Tokenize → sample logits → forward → softmax → detokenize.' },
      { id: 'tokenize', label: 'Tokenize → forward tạo logits → softmax thành xác suất → sample → detokenize.', isCorrect: true },
      { id: 'detokenize', label: 'Detokenize prompt → softmax text → forward → sample.' },
      { id: 'probabilities', label: 'Forward raw text → sample mọi vị trí → tokenize output.' },
    ],
    success: 'Đúng. Model forward trả logits; softmax/decoding biến logits cuối thành phân phối hợp lệ trước khi chọn token và giải mã.',
    error: 'Hãy theo dõi type của dữ liệu: text → IDs → logits → probabilities → token ID → text.',
  },
  {
    id: 'sampling-role', title: 'Vai trò của sampling', prompt: 'Sampling làm gì trong pipeline?', mode: 'single',
    options: [
      { id: 'argmax-logit', label: 'Chọn logit lớn nhất trước khi các score được chuẩn hóa.' },
      { id: 'choose', label: 'Chọn một token từ phân phối xác suất theo chiến lược decoding.', isCorrect: true },
      { id: 'normalize', label: 'Chuẩn hóa logits thành phân phối xác suất trên vocabulary.' },
      { id: 'text', label: 'Biến token đã chọn thành chuỗi text để hiển thị.' },
    ],
    success: 'Chính xác. Sampling quyết định token nào được lấy từ phân phối xác suất.',
    error: 'Chưa đúng. Sampling xảy ra sau khi model đã tạo phân phối xác suất.',
  },
  {
    id: 'corpus-vocabulary', title: 'Corpus và vocabulary', prompt: 'Mối quan hệ nào giữa corpus và vocabulary được trình bày trong bài?', mode: 'single',
    options: [
      { id: 'repeated-tokens', label: 'Vocabulary là corpus sau khi bỏ thứ tự các câu nhưng vẫn giữ mọi token lặp.' },
      { id: 'reversed', label: 'Corpus là danh sách token duy nhất mà model có thể output.' },
      { id: 'sentence-table', label: 'Vocabulary lưu các câu training cùng xác suất xuất hiện của chúng.' },
      { id: 'unique-tokens', label: 'Corpus là tập văn bản huấn luyện; các token duy nhất được lấy ra để tạo vocabulary.', isCorrect: true },
    ],
    success: 'Đúng. Corpus chứa văn bản; vocabulary là tập token duy nhất được xây dựng từ dữ liệu đó.',
    error: 'Chưa đúng. Hãy phân biệt tập văn bản dùng để train với danh sách token mà model biết.',
  },
  {
    id: 'output-vector-length', title: 'Độ dài output vector', prompt: 'Nếu vocabulary có 50.000 token thì output vector cho vị trí tiếp theo có bao nhiêu phần tử?', mode: 'single',
    options: [
      { id: 'hidden-size', label: 'd phần tử, bằng kích thước context vector trước output head.' },
      { id: 'per-context-token', label: '50.000 × số token trong context, vì mỗi vị trí cần một score cho mỗi token.' },
      { id: 'vocab-size', label: '50.000 phần tử, mỗi phần tử ứng với một token.', isCorrect: true },
      { id: 'decoded-size', label: 'Bằng số token còn lại sau khi áp dụng chiến lược decoding.' },
    ],
    success: 'Đúng. Output vector có một vị trí cho mỗi token trong vocabulary.',
    error: 'Chưa đúng. Độ dài vector phụ thuộc vocabulary size, không phụ thuộc số câu trong corpus hay độ dài context.',
  },
  {
    id: 'logits-vs-probabilities', title: 'Logits', prompt: 'Điểm khác nhau đúng giữa logits và probabilities trong pipeline là gì?', mode: 'single',
    options: [
      { id: 'normalized', label: 'Logits là các score chưa chuẩn hóa; sau softmax chúng trở thành probabilities có tổng bằng 1.', isCorrect: true },
      { id: 'same-values', label: 'Logits và probabilities là cùng một vector; softmax chỉ đổi tên các phần tử.' },
      { id: 'selected-token', label: 'Logits chỉ chứa token đã được chọn, còn probabilities chứa toàn bộ input.' },
      { id: 'logits-text', label: 'Logits là text đầu ra, còn probabilities là token IDs của input.' },
    ],
    success: 'Đúng. Forward tạo logits; softmax chuẩn hóa chúng thành phân phối xác suất.',
    error: 'Hãy nhìn lại thứ tự: forward → logits → softmax → probabilities.',
  },
]} />
