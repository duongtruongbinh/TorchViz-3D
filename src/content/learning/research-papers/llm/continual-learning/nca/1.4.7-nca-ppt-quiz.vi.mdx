export const lessonMetadata = {
  domainId: 'research-papers',
  id: 'nca-ppt-quiz',
  locale: 'vi',
  title: 'Quiz: NCA Pre-Pre-Training (2026)',
  headings: [
    'Mục tiêu của NCA Pre-Pre-Training',
    'Suy luận quy luật ẩn dưới góc nhìn Bayes',
    'Epiplexity và quan sát viên bị giới hạn',
    'Nhiệt độ trong chuyển trạng thái NCA',
    'Gzip như thước đo độ phức tạp',
    'Mã hóa Patch 2x2',
    'So sánh 164M NCA với 1.6B C4',
    'Ý nghĩa của Token Efficiency Gain',
    'Kết quả ablation Attention và MLP',
    'Nguyên lý Complexity Matching',
    'Ảnh hưởng của kích thước bảng chữ cái',
    'Giới hạn suy luận cho Continual Learning',
  ],
  keywords: [
    'NCA Quiz',
    'Pre-Pre-Training',
    'Bayesian In-Context Learning',
    'Epiplexity',
    'Gzip Complexity',
    'Token Efficiency',
    'Attention vs MLP',
    'Continual Learning',
  ],
  conceptIds: [
    'nca-ppt-concept-role',
    'bayesian-latent-rule-inference',
    'epiplexity-bounded-observer',
    'nca-dynamics-formula',
    'gzip-complexity-filtering',
    'patch-tokenization-encoding',
    'empirical-nca-vs-c4',
    'token-efficiency-metric',
    'attention-vs-mlp-ablation',
    'complexity-matching-principle',
    'alphabet-size-paradox',
    'continual-learning-parameter-isolation',
  ],
  pageCount: 12,
};

<MdxQuiz id="nca-ppt-quiz" questions={[
  {
    id: 'nca-ppt-concept-role',
    title: 'Mục tiêu của NCA Pre-Pre-Training',
    prompt: 'Mục tiêu trực tiếp của NCA Pre-Pre-Training trong bài báo là gì?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Dạy embedding ghi nhớ từ vựng, cú pháp và tri thức tự nhiên trước giai đoạn Pre-Training chính.' },
      { id: 'opt2', label: 'Học các prior tính toán có thể chuyển giao từ chuỗi động lực học phi ngôn ngữ.', isCorrect: true },
      { id: 'opt3', label: 'Thay toàn bộ dữ liệu văn bản bằng NCA trong mọi giai đoạn huấn luyện mô hình.' },
      { id: 'opt4', label: 'Giảm số tầng Transformer để mô hình khớp với kích thước lưới tế bào.' },
    ],
    success: 'Đúng. Pha NCA đứng trước Pre-Training ngôn ngữ và nhằm tạo các prior tính toán có ích cho việc học văn bản và tác vụ suy luận sau đó.',
    error: 'NCA PPT không dạy từ vựng, không giảm kiến trúc và chưa thay thế Pre-Training ngôn ngữ. Nó tạo tín hiệu cấu trúc trước khi mô hình học văn bản.',
  },
  {
    id: 'bayesian-latent-rule-inference',
    title: 'Suy luận quy luật ẩn dưới góc nhìn Bayes',
    prompt: 'Vì sao next-token prediction trên nhiều quỹ đạo NCA phù hợp với cách nhìn Bayesian In-Context Learning?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Mỗi token đã mã hóa sẵn toàn bộ trọng số của mạng chuyển trạng thái, nên không cần dùng ngữ cảnh.' },
      { id: 'opt2', label: 'Mọi quỹ đạo dùng chung một quy luật cố định, vì thế mô hình chỉ cần ghi nhớ một bảng chuyển trạng thái.' },
      { id: 'opt3', label: 'Posterior của mô hình được tính chính xác bằng thuật toán MCMC bên ngoài Transformer trước mỗi dự đoán.' },
      { id: 'opt4', label: 'Mỗi chuỗi có quy luật ẩn riêng, nên mô hình phải dùng ngữ cảnh để suy ra quy luật rồi dự đoán tiếp.', isCorrect: true },
    ],
    success: 'Đúng. Mỗi tham số $\theta$ được lấy mẫu tạo một dynamics rule khác nhau; dự đoán tiếp đòi hỏi suy ra rule đang chi phối chính chuỗi hiện tại.',
    error: 'Điểm cốt lõi là quy luật thay đổi giữa các chuỗi nhưng nhất quán trong từng chuỗi. Ngữ cảnh cung cấp bằng chứng để mô hình suy ra quy luật ẩn.',
  },
  {
    id: 'epiplexity-bounded-observer',
    title: 'Epiplexity và quan sát viên bị giới hạn',
    prompt: 'Cách diễn giải nào đúng nhất về vai trò của Epiplexity trong lập luận của bài học?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Quy tắc sinh có thể đơn giản nhưng rollout dài vẫn tạo cấu trúc hữu ích đối với một quan sát viên bị giới hạn tính toán.', isCorrect: true },
      { id: 'opt2', label: 'Hàm tất định làm tăng mutual information tuyệt đối theo thời gian, nên Bất đẳng thức Xử lý Dữ liệu không còn áp dụng cho bất kỳ NCA nào.' },
      { id: 'opt3', label: 'Epiplexity chứng minh mọi mẫu phức tạp do cellular automata sinh ra đều cải thiện khả năng suy luận của LLM.' },
      { id: 'opt4', label: 'Chỉ cần tăng entropy của chuỗi đến mức tối đa là chắc chắn thu được cấu trúc có thể chuyển giao sang ngôn ngữ.' },
    ],
    success: 'Đúng. Epiplexity nhấn mạnh khoảng cách giữa mô tả sinh ngắn và cấu trúc khó khai thác đối với tác nhân có ngân sách tính toán hữu hạn.',
    error: 'Epiplexity không phủ định DPI và không bảo đảm mọi chuỗi phức tạp đều hữu ích. Nó mô tả độ phức tạp biểu kiến đối với quan sát viên bị giới hạn.',
  },
  {
    id: 'nca-dynamics-formula',
    title: 'Nhiệt độ trong chuyển trạng thái NCA',
    prompt: 'Trong phép lấy mẫu trạng thái kế tiếp, $\tau = 10^{-3}$ có tác dụng nào?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Chuẩn hóa trọng số của Transformer để ổn định gradient trong pha học ngôn ngữ.' },
      { id: 'opt2', label: 'Biến trạng thái rời rạc thành vector liên tục được lưu nguyên vào chuỗi huấn luyện.' },
      { id: 'opt3', label: 'Làm phân phối Softmax rất sắc nhưng vẫn giữ một mức stochasticity nhỏ khi lấy mẫu.', isCorrect: true },
      { id: 'opt4', label: 'Cho phép backpropagation tối ưu trực tiếp từng transition rule NCA ngẫu nhiên trong lúc sinh dữ liệu.' },
    ],
    success: 'Đúng. Paper mô tả nhiệt độ thấp này làm lựa chọn gần tất định nhưng vẫn có stochasticity nhẹ; các transition rule được lấy mẫu ngẫu nhiên để sinh dữ liệu.',
    error: 'Nhiệt độ thuộc quá trình lấy mẫu trạng thái NCA. Nó không phải hệ số tối ưu Transformer và không được dùng để huấn luyện transition rule bằng gradient.',
  },
  {
    id: 'gzip-complexity-filtering',
    title: 'Gzip như thước đo độ phức tạp',
    prompt: 'Ngưỡng gzip $r > 50\%$ được dùng như thế nào trong pipeline chính?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Bảo đảm mọi quỹ đạo được giữ lại đều nằm chính xác tại Edge of Chaos, có cùng entropy và không chứa nhiễu khó dự đoán.' },
      { id: 'opt2', label: 'Giữ các quỹ đạo khó nén hơn như một dải complexity cao theo proxy thực dụng của Kolmogorov complexity.', isCorrect: true },
      { id: 'opt3', label: 'Giảm kích thước file huấn luyện xuống dưới một nửa trước khi truyền dữ liệu sang GPU.' },
      { id: 'opt4', label: 'Loại mọi quỹ đạo có entropy cao rồi chỉ giữ các fixed point và chu kỳ lặp ngắn.' },
    ],
    success: 'Đúng. Tỷ lệ gzip là proxy thực dụng cho intrinsic sequence complexity; pipeline mặc định giữ các trajectory thuộc dải 50%+.',
    error: 'Gzip được dùng để chọn dải độ phức tạp, không phải tối ưu dung lượng lưu trữ. Paper không đồng nhất ngưỡng 50%+ với một bảo đảm Edge of Chaos.',
  },
  {
    id: 'patch-tokenization-encoding',
    title: 'Mã hóa Patch 2x2',
    prompt: 'Với 10 trạng thái tế bào, một patch $2 \times 2$ tạo bảng từ vựng có bao nhiêu token khả dĩ?',
    mode: 'single',
    options: [
      { id: 'opt1', label: '144 token, vì lưới $12 \times 12$ có tổng cộng 144 tế bào tại mỗi timestep.' },
      { id: 'opt2', label: '36 token, vì một timestep được chia thành 36 patch không chồng lấn trên lưới.' },
      { id: 'opt3', label: '50.257 token, vì pipeline dùng nguyên bảng từ vựng BPE tiêu chuẩn của GPT-2.' },
      { id: 'opt4', label: '$10^4 = 10{,}000$ token, vì patch có bốn ô và mỗi ô có 10 trạng thái.', isCorrect: true },
    ],
    success: 'Đúng. Có $10 \times 10 \times 10 \times 10 = 10^4$ tổ hợp patch. Con số 36 là số token được tạo từ một lưới, không phải vocabulary size.',
    error: 'Hãy phân biệt số patch trong một timestep với số cấu hình patch khả dĩ. Vocabulary bijective chứa $10^4$ token.',
  },
  {
    id: 'empirical-nca-vs-c4',
    title: 'So sánh 164M NCA với 1.6B C4',
    prompt: 'Kết luận nào phản ánh đúng nhất thí nghiệm NCA 164M tokens so với C4 1.6B tokens?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'NCA luôn tốt hơn văn bản tự nhiên ở mọi quy mô và vì thế không còn cần Pre-Training trên ngôn ngữ.' },
      { id: 'opt2', label: 'C4 thua vì dùng kiến trúc Transformer nhỏ hơn và không được tối ưu hyperparameter công bằng.' },
      { id: 'opt3', label: 'NCA tốt hơn trong thiết lập này; tác giả giả thuyết NCA rèn latent-rule inference hiệu quả hơn ở ngân sách nhỏ.', isCorrect: true },
      { id: 'opt4', label: 'Kết quả chứng minh causal rằng semantic shortcuts là nguyên nhân duy nhất làm C4 kém hơn trong mọi miền dữ liệu.' },
    ],
    success: 'Đúng. Kết quả 5% perplexity và 1.4× convergence là thực nghiệm; lời giải thích về shallow patterns và latent-rule inference được tác giả nêu như giả thuyết.',
    error: 'Cần tách kết quả đo được khỏi lời giải thích cơ chế. Paper không kết luận NCA thay thế hoàn toàn ngôn ngữ hoặc chứng minh một nguyên nhân duy nhất.',
  },
  {
    id: 'token-efficiency-metric',
    title: 'Ý nghĩa của Token Efficiency Gain',
    prompt: 'Token Efficiency Gain 49% trên CodeParrot biểu thị điều gì?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Tổng token PPT và PT cần để đạt loss cuối của scratch thấp hơn 49% so với baseline.', isCorrect: true },
      { id: 'opt2', label: 'VRAM cần cho một forward pass thấp hơn 49% vì patch tokenization nén activation trung gian.' },
      { id: 'opt3', label: 'Context length tăng 49% mà không cần thay positional encoding hoặc attention mask.' },
      { id: 'opt4', label: 'HumanEval pass@1 tăng tuyệt đối 49 điểm phần trăm so với mô hình được train từ đầu.' },
    ],
    success: 'Đúng. Metric cộng ngân sách token của hai pha và đo phần tiết kiệm để đạt mức loss cuối của scratch baseline.',
    error: 'Token Efficiency Gain đo ngân sách token huấn luyện, không đo VRAM, context length hay mức tăng điểm benchmark.',
  },
  {
    id: 'attention-vs-mlp-ablation',
    title: 'Kết quả ablation Attention và MLP',
    prompt: 'Selective re-initialization cho thấy kết luận nào được dữ liệu hỗ trợ?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Chỉ Attention mang toàn bộ transfer gain; MLP và LayerNorm luôn gây hại trên mọi downstream domain.' },
      { id: 'opt2', label: 'MLP là nơi duy nhất chứa latent-rule inference vì reset Attention hầu như không thay đổi perplexity.' },
      { id: 'opt3', label: 'Mọi module đóng góp ngang nhau, nên re-initialize riêng từng nhóm trọng số cho kết quả giống hệt nhau.' },
      { id: 'opt4', label: 'Attention mang tín hiệu chuyển giao mạnh nhất; tác động của MLP và LayerNorm phụ thuộc miền đích.', isCorrect: true },
    ],
    success: 'Đúng. Reset Attention gây suy giảm lớn nhất ở các cấu hình; MLP và LayerNorm gây interference trên OpenWebText nhưng gần như trung tính trên CodeParrot.',
    error: 'Paper kết luận Attention là thành phần chuyển giao mạnh nhất, không phải thành phần duy nhất. Hiệu ứng của MLP và LayerNorm thay đổi theo miền.',
  },
  {
    id: 'complexity-matching-principle',
    title: 'Nguyên lý Complexity Matching',
    prompt: 'Kết quả quét các dải gzip cho thấy quan hệ nào giữa dữ liệu NCA và miền đích?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Dải 50%+ luôn tối ưu vì dữ liệu khó nén nhất luôn tạo transfer tốt nhất cho mọi loại corpus.' },
      { id: 'opt2', label: 'CodeParrot đạt sweet spot ở 30–40%, còn web text hưởng lợi từ NCA phức tạp hơn ở 50%+.', isCorrect: true },
      { id: 'opt3', label: 'Gzip của corpus đích quyết định trực tiếp learning rate tối ưu và số attention heads cần dùng.' },
      { id: 'opt4', label: 'Miền đích càng dễ nén thì bắt buộc phải tăng alphabet size của NCA lên 15 trạng thái.' },
    ],
    success: 'Đúng. Optimum phụ thuộc miền: CodeParrot dễ nén hơn và hợp dải trung gian, trong khi OpenWebText và OpenWebMath hợp dải complexity cao hơn.',
    error: 'Complexity Matching là kết quả theo miền, không phải quy tắc “càng phức tạp càng tốt” và không trực tiếp chọn kiến trúc Transformer.',
  },
  {
    id: 'alphabet-size-paradox',
    title: 'Ảnh hưởng của kích thước bảng chữ cái',
    prompt: 'Paper diễn giải kết quả $n=2$ scaling tốt hơn $n=10,15$ như thế nào?',
    mode: 'single',
    options: [
      { id: 'opt1', label: '$n=2$ làm embedding chỉ còn hai chiều và vì thế giảm gần như toàn bộ chi phí attention, MLP lẫn huấn luyện Transformer.' },
      { id: 'opt2', label: 'Transformer chỉ biểu diễn được hàm Boolean nên mọi alphabet lớn hơn hai đều gây lỗi kiến trúc.' },
      { id: 'opt3', label: 'Không gian bị ràng buộc hơn có thể tập trung mẫu vào dynamics chuyển giao ổn định; đây là giả thuyết từ kết quả.', isCorrect: true },
      { id: 'opt4', label: '$n=2$ chỉ có đúng $2^9$ transition rule vì mỗi neighborhood ánh xạ trực tiếp sang một bit duy nhất.' },
    ],
    success: 'Đúng. Dữ liệu cho thấy alphabet nhỏ tiếp tục cải thiện khi alphabet lớn plateau; tác giả đề xuất việc ràng buộc không gian có thể giúp lấy mẫu cấu trúc nhất quán hơn.',
    error: 'Transition rule là mạng nơ-ron với trọng số liên tục, không phải bảng hữu hạn $2^9$ luật. Lời giải thích về concentration là giả thuyết, không phải chứng minh.',
  },
  {
    id: 'continual-learning-parameter-isolation',
    title: 'Giới hạn suy luận cho Continual Learning',
    prompt: 'Từ kết quả Attention–MLP, kết luận nào thận trọng nhất cho Continual Learning?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Bảo vệ Attention và dành plasticity cho module đặc thù miền là một hướng thiết kế cần được kiểm chứng thêm.', isCorrect: true },
      { id: 'opt2', label: 'Paper đã chứng minh đóng băng Attention rồi chỉ train MLP loại bỏ hoàn toàn catastrophic forgetting trên nhiều miền tuần tự.' },
      { id: 'opt3', label: 'Có thể xóa MLP sau mỗi task mà không mất tri thức, vì MLP không đóng góp cho bất kỳ downstream domain nào.' },
      { id: 'opt4', label: 'Chỉ cần phát lại quỹ đạo NCA 50%+ là đủ bảo đảm stability–plasticity mà không cần replay dữ liệu hay regularization.' },
    ],
    success: 'Đúng. Ablation gợi ý một hướng phân lập vai trò, nhưng paper không chạy benchmark Continual Learning tuần tự để xác nhận giải pháp chống quên.',
    error: 'Hãy phân biệt implication với kết quả đã kiểm chứng. Thiết kế Continual Learning là ngoại suy hợp lý, chưa phải đóng góp thực nghiệm của paper.',
  },
]} />
