export const lessonMetadata = {
  domainId: 'research-papers',
  id: 'sdc-lora-quiz',
  locale: 'en',
  title: 'Quiz: Abstract',
  headings: [
    'Fundamental Concept of SVD',
    'Rationale for SVD Approach',
    'The LoRA Forgetting Paradox',
    'Core Knowledge Storage',
    'Singular-Subspace Drift Phenomenon',
    'Scope of Subspace Drift',
    'Spectral Calibration Mechanism',
    'Inference Overhead of SDC-LoRA',
    'Empirical Benchmark Design',
    'Empirical Results of SDC-LoRA',
  ],
  keywords: ['SDC-LoRA', 'Continual Learning', 'LoRA', 'Catastrophic Forgetting', 'SVD', 'Spectral Calibration', 'MMLU', 'GSM8K', 'HumanEval', 'Quiz'],
  conceptIds: [
    'svd-fundamental-concept',
    'why-svd-approach',
    'lora-forgetting-paradox',
    'principal-singular-subspace-knowledge',
    'singular-subspace-drift-concept',
    'drift-scope-u-and-v',
    'spectral-calibration-mechanism',
    'zero-inference-overhead',
    'empirical-benchmark-setup',
    'empirical-results-comparison',
  ],
  pageCount: 10,
};

<MdxQuiz id="sdc-lora-quiz" questions={[
  {
    id: 'svd-fundamental-concept',
    title: 'Fundamental Concept of SVD',
    prompt: 'In linear algebra and machine learning analysis, what does Singular Value Decomposition ($W_0 = U \\Sigma V^T$) represent?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Decomposes a matrix into an input feature basis ($V$), an output representation basis ($U$), and an energy/scaling spectrum ($\\Sigma$).', isCorrect: true },
      { id: 'opt2', label: 'Splits a matrix into upper and lower triangular matrices (LU decomposition).' },
      { id: 'opt3', label: 'Directly computes the matrix inverse to solve linear systems.' },
      { id: 'opt4', label: 'Forces all off-diagonal elements to zero using Gaussian elimination.' },
    ],
    success: 'Correct! SVD decomposes $W_0 = U \\ Sigma V^T$ into orthonormal basis matrices $V$ (input), $U$ (output), and diagonal matrix $\\Sigma$ acting as an energy gauge.',
    error: 'Incorrect. Remember the SVD breakdown $W_0 = U \\Sigma V^T$: $V$ is the input basis, $U$ is the output basis, and $\\Sigma$ reflects the energy magnitude along each direction.',
  },
  {
    id: 'why-svd-approach',
    title: 'Rationale for SVD Approach',
    prompt: 'Why did the authors choose Singular Value Decomposition (SVD) as the core analytical framework for catastrophic forgetting in LoRA?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'SVD decomposes weight matrices into orthonormal coordinate systems with hierarchically ordered energy levels.', isCorrect: true },
      { id: 'opt2', label: 'Because SVD is the only algorithm capable of training neural networks without backpropagation.' },
      { id: 'opt3', label: 'Because SVD compresses weight matrices to 1-bit representations for memory efficiency.' },
      { id: 'opt4', label: 'Because the authors aimed to replace Transformer matrix multiplication with LU decomposition.' },
    ],
    success: 'Correct! By decomposing weight matrices into orthonormal coordinate systems with ordered energy levels, SVD enables clean, independent separation of principal directions (preserving prior knowledge) and minor directions (accommodating new tasks) for precise gradient modulation.',
    error: 'Incorrect. Remember that SVD provides an orthonormal coordinate system with hierarchically ordered energy levels.',
  },
  {
    id: 'lora-forgetting-paradox',
    title: 'The LoRA Forgetting Paradox',
    prompt: 'Despite LoRA updating only a tiny fraction of low-rank parameters (< 1% while freezing $W_0$), why does the model still suffer from severe Catastrophic Forgetting?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Because even a slight angular drift in the principal singular directions is amplified by large singular values, drastically distorting prior knowledge representations.', isCorrect: true },
      { id: 'opt2', label: 'Because freezing W0 triggers GPU memory overflows that delete previous task weights.' },
      { id: 'opt3', label: 'Because low-rank matrices are mathematically incapable of storing float32 values.' },
      { id: 'opt4', label: 'Because AdamW resets all Transformer layer states every 100 optimization steps.' },
    ],
    success: 'Correct! Even with minimal parameters, when Subspace Drift occurs along the principal directions (associated with large singular values $\\sigma_i$), even small angular perturbations are heavily amplified across Transformer layers, severely distorting historical task representations.',
    error: 'Incorrect. Principal singular directions carry immense energy, so even subtle angular rotations induced by low-rank adapters cause severe representation distortion.',
  },
  {
    id: 'principal-singular-subspace-knowledge',
    title: 'Core Knowledge Storage',
    prompt: 'According to SVD analysis in the paper, where is pre-trained knowledge predominantly concentrated within base weight matrix $W_0$?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'In the Principal Singular Subspace corresponding to the largest singular values.', isCorrect: true },
      { id: 'opt2', label: 'Uniformly distributed across all parametric directions.' },
      { id: 'opt3', label: 'Exclusively in the minor subspace with smallest singular values.' },
      { id: 'opt4', label: 'Only in Feed-Forward layers and absent from Attention projections.' },
    ],
    success: 'Correct! Foundational pre-trained capabilities reside in the Principal Singular Subspace with top singular values $\\sigma_i$.',
    error: 'Incorrect. Top singular values carry the dominant energy and foundational skills.',
  },
  {
    id: 'singular-subspace-drift-concept',
    title: 'Singular-Subspace Drift Phenomenon',
    prompt: 'How is the "Singular-Subspace Drift" (SD) phenomenon in LoRA fine-tuning accurately understood?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Instead of staying confined to the Minor Subspace, LoRA updates spill over and rotate the Principal Subspace (where prior knowledge resides).', isCorrect: true },
      { id: 'opt2', label: 'LoRA rank r automatically expands after every training epoch.' },
      { id: 'opt3', label: 'Optimizer learning rate drops abruptly to zero when switching tasks.' },
      { id: 'opt4', label: 'Gradients of A and B cancel out completely in the backward pass.' },
    ],
    success: 'Correct! Instead of updating safely within the Minor Subspace (free capacity), unconstrained LoRA gradient dynamics spill over and distort the Principal Subspace, leading to catastrophic forgetting of pre-trained capabilities.',
    error: 'Incorrect. Drift occurs when LoRA updates fail to stay confined to the Minor Subspace and end up perturbing the Principal Subspace.',
  },
  {
    id: 'drift-scope-u-and-v',
    title: 'Scope of Subspace Drift',
    prompt: 'When Singular-Subspace Drift occurs, which components of the SVD system undergo angular distortion?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Both the input feature subspace (Right Singular Vectors V) and output representation subspace (Left Singular Vectors U).', isCorrect: true },
      { id: 'opt2', label: 'Only input vectors V without affecting output vectors U.' },
      { id: 'opt3', label: 'Only the diagonal singular value matrix Sigma without rotating vectors.' },
      { id: 'opt4', label: 'Only the token embedding layer.' },
    ],
    success: 'Correct! Both $V$ (input feature recognition) and $U$ (output forward propagation) undergo directional drift.',
    error: 'Incorrect. Recall the 2D example: both $V$ and $U$ undergo simultaneous angular drift.',
  },
  {
    id: 'spectral-calibration-mechanism',
    title: 'Spectral Calibration Mechanism',
    prompt: 'How does the Spectral Calibration factor ($\\gamma_{\\text{sc}}$) in SDC-LoRA mitigate knowledge forgetting?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'By selectively downscaling gradients along the principal singular subspace of W0 while leaving minor-subspace updates intact.', isCorrect: true },
      { id: 'opt2', label: 'By completely freezing gradients across the entire model.' },
      { id: 'opt3', label: 'By doubling the learning rate on principal singular directions.' },
      { id: 'opt4', label: 'By forcing LoRA rank r to match the Transformer hidden dimension.' },
    ],
    success: 'Correct! $\\gamma_{\\text{sc}}$ constrains energy growth along principal directions by downscaling gradients, while minor subspace freely absorbs new tasks.',
    error: 'Incorrect. Spectral Calibration uses $\\gamma_{\\text{sc}}$ to selectively downscale gradients along the principal subspace.',
  },
  {
    id: 'zero-inference-overhead',
    title: 'Inference Overhead of SDC-LoRA',
    prompt: 'What is the deployment inference overhead of a model adapted with SDC-LoRA compared to the base model?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Zero Inference Overhead because adapter updates $\\Delta W$ are directly merged into W0 (W = W0 + Delta W).', isCorrect: true },
      { id: 'opt2', label: 'Doubled latency due to real-time SVD decomposition during every forward pass.' },
      { id: 'opt3', label: 'Extra memory overhead for storing full U and V vector systems in GPU VRAM.' },
      { id: 'opt4', label: '50% slower Time-to-First-Token.' },
    ],
    success: 'Correct! All SDC-LoRA mechanisms operate during training. At deployment, $W = W_0 + \\Delta W$ merges seamlessly with zero latency or extra parameters.',
    error: 'Incorrect. A primary practical benefit of SDC-LoRA is Zero Inference Overhead.',
  },
  {
    id: 'empirical-benchmark-setup',
    title: 'Empirical Benchmark Design',
    prompt: 'How did the authors divide the evaluation benchmarks into two independent groups to rigorously test continual learning performance?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'General foundational benchmarks (MMLU, TruthfulQA, HellaSwag) to measure forgetting, and domain-specific benchmarks (GSM8K, HumanEval) to measure new task mastery.', isCorrect: true },
      { id: 'opt2', label: 'Benchmarks executed on CPU hardware versus benchmarks executed on GPU hardware.' },
      { id: 'opt3', label: 'User typing speed tests versus disk storage footprint measurements.' },
      { id: 'opt4', label: 'English language comprehension tests versus binary machine code verification.' },
    ],
    success: 'Correct! The paper decouples evaluation into pre-trained general reasoning benchmarks (MMLU, TruthfulQA, HellaSwag) and downstream specialized task benchmarks (GSM8K, HumanEval) to avoid data contamination.',
    error: 'Incorrect. The evaluation requires two independent benchmark sets: (1) general pre-trained knowledge to detect forgetting, and (2) downstream tasks to evaluate new learning.',
  },
  {
    id: 'empirical-results-comparison',
    title: 'Empirical Results of SDC-LoRA',
    prompt: 'What key empirical advantage does SDC-LoRA demonstrate over standard LoRA across experiments with LLaMA-3.1-8B-Instruct and Qwen2.5-7B-Chat?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'SDC-LoRA almost entirely preserves general reasoning capabilities (MMLU, TruthfulQA, HellaSwag) while matching or outperforming standard LoRA on newly adapted tasks (GSM8K, HumanEval).', isCorrect: true },
      { id: 'opt2', label: 'SDC-LoRA sacrifices 50% of MMLU score to slightly improve coding benchmarks.' },
      { id: 'opt3', label: 'SDC-LoRA only functions effectively on single-task adaptation and degrades on sequential tasks.' },
      { id: 'opt4', label: 'SDC-LoRA triples the number of trainable parameters compared to full fine-tuning.' },
    ],
    success: 'Correct! While standard LoRA causes severe forgetting on general benchmarks after sequential fine-tuning, SDC-LoRA halts knowledge degradation (>99% retained) while excelling at mathematics (GSM8K) and code generation (HumanEval).',
    error: 'Incorrect. SDC-LoRA achieves a dual win: retaining pre-trained foundational knowledge (MMLU/TruthfulQA/HellaSwag) while maximizing performance on new tasks (GSM8K/HumanEval).',
  },
]} />
