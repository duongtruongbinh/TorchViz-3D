export const lessonMetadata = {
  domainId: 'continual-learning-llm',
  id: 'continual-learning-llm-overview-quiz',
  locale: 'vi',
  title: "Quiz",
  headings: [
    'Đặc điểm LLM truyền thống',
    'Hạn chế trong môi trường thực tế',
    'Lý do không huấn luyện lại từ đầu',
    'RAG và Continual Learning',
    'Mục tiêu cốt lõi của Continual Learning',
    'Nhận diện acquisition và forgetting',
    'Nhận diện quy trình Continual Learning',
    'Baseline khi có toàn bộ lịch sử',
  ],
  keywords: ['LLM', 'Continual Learning', 'RAG', 'Pre-trained', 'Fine-tune', 'Quiz'],
  conceptIds: ['statically-pretrained-nature', 'why-static-llm-insufficient', 'why-not-retrain-from-scratch', 'rag-vs-continual-learning', 'cl-core-goal', 'cl-core-challenge', 'lifelong-learning-analogy', 'method-choice'],
  pageCount: 8,
}

<MdxQuiz id="continual-learning-llm-overview-quiz" questions={[
  {
    id: 'statically-pretrained-nature',
    title: 'Đặc điểm LLM truyền thống',
    prompt: 'Đặc điểm chính của các mô hình LLM được pre-train thông thường (statically pre-trained) là gì?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Được pre-train trên một tập dữ liệu cố định tại một thời điểm nhất định.', isCorrect: true },
      { id: 'opt2', label: 'Được cập nhật trọng số định kỳ bằng mọi tương tác sau khi triển khai.' },
      { id: 'opt3', label: 'Chỉ giữ kiến thức trong retrieval index thay vì trong tham số mô hình.' },
      { id: 'opt4', label: 'Được huấn luyện lại từ đầu mỗi khi phân phối dữ liệu thay đổi.' },
    ],
    success: 'Chính xác. Các LLM truyền thống được pre-train trên tập dữ liệu cố định tại một thời điểm (statically pre-trained).',
    error: 'Chưa chính xác. Hãy nhớ về khái niệm statically pre-trained của LLM.',
  },
  {
    id: 'why-static-llm-insufficient',
    title: 'Hạn chế trong môi trường thực tế',
    prompt: 'Vì sao một LLM được pre-train cố định lại không đủ để đáp ứng nhu cầu trong môi trường thực tế?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Vì retrieval đưa context mới vào inference nên checkpoint cũng hấp thụ tri thức đó vào weights.' },
      { id: 'opt3', label: 'Vì chỉ phân phối token thay đổi theo thời gian, còn fact và nhu cầu downstream giữ nguyên.' },
      { id: 'opt4', label: 'Vì mở rộng context window tương đương cập nhật checkpoint theo các sự kiện mới.' },
      { id: 'opt2', label: 'Vì dữ liệu, kiến thức chuyên ngành và các sự kiện mới trong thực tế liên tục thay đổi theo thời gian.', isCorrect: true },
    ],
    success: 'Chính xác! Trong thực tế, sự kiện, tri thức ngành và nhu cầu người dùng liên tục biến đổi, làm cho kiến thức cố định tại thời điểm pre-train bị lạc hậu theo thời gian.',
    error: 'Chưa chính xác. Hãy cân nhắc về tính biến đổi liên tục của dữ liệu và nhu cầu thực tế.',
  },
  {
    id: 'why-not-retrain-from-scratch',
    title: 'Lý do không huấn luyện lại từ đầu',
    prompt: 'Vì sao không gom toàn bộ dữ liệu cũ và mới rồi huấn luyện (pre-train) lại LLM từ đầu mỗi khi có dữ liệu mới?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Vì huấn luyện chung dữ liệu cũ và mới luôn làm giảm hiệu năng trên dữ liệu mới.' },
      { id: 'opt3', label: 'Vì chi phí compute và thời gian pre-train lại từ đầu quá lớn khi dữ liệu thay đổi liên tục.', isCorrect: true },
      { id: 'opt2', label: 'Vì checkpoint cũ không thể dùng làm điểm khởi đầu cho một lần cập nhật mới.' },
      { id: 'opt4', label: 'Vì full-history retraining chỉ phù hợp khi dữ liệu mới có cùng phân phối với dữ liệu cũ.' },
    ],
    success: 'Chính xác! Pre-train lại LLM từ đầu tốn chi phí GPU và thời gian rất lớn, tạo động lực cho các cách cập nhật hiệu quả hơn.',
    error: 'Chưa đúng. Hãy cân nhắc về yếu tố chi phí tính toán và tài nguyên GPU.',
  },
  {
    id: 'rag-vs-continual-learning',
    title: 'RAG và Continual Learning',
    prompt: 'Một team muốn model duy trì policy trả lời mới ngay cả khi hệ thống không truy xuất được tài liệu. Trường hợp này cần Continual Learning giải quyết điều gì mà RAG không thể?',
    mode: 'single',
    options: [
      { id: 'opt3', label: 'Cập nhật model để policy mới trở thành hành vi bền vững, đồng thời kiểm tra model còn giữ các năng lực cũ.', isCorrect: true },
      { id: 'opt1', label: 'Đưa policy vào context nhiều lần để model tự ghi policy đó vào backbone mà không cần cập nhật tham số.' },
      { id: 'opt2', label: 'Bảo đảm retrieval luôn trả về đúng tài liệu để RAG trở thành một hình thức model adaptation.' },
      { id: 'opt4', label: 'Giảm kích thước retrieval index để policy mới vừa với context window của model.' },
    ],
    success: 'Đúng. Continual Learning cập nhật model để hình thành hành vi bền vững và phải kiểm tra retention; RAG chỉ cung cấp thêm context tại thời điểm suy luận.',
    error: 'Hãy phân biệt phần được thay đổi: context của lần suy luận hay năng lực và hành vi của model.',
  },
  {
    id: 'cl-core-goal',
    title: 'Mục tiêu cốt lõi của Continual Learning',
    prompt: 'Continual Learning cho LLM đặt ra mục tiêu cốt lõi nào?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Giữ nguyên toàn bộ hành vi cũ, kể cả khi tri thức cũ đã lỗi thời.' },
      { id: 'opt2', label: 'Tối đa hóa hiệu năng trên stage mới mà không đặt ràng buộc cho stage trước.' },
      { id: 'opt4', label: 'Tiếp thu kiến thức hoặc nhiệm vụ mới hiệu quả mà vẫn giữ các năng lực cũ cần thiết.', isCorrect: true },
      { id: 'opt3', label: 'Thay mọi lần cập nhật bằng retrieval để không bao giờ thay đổi tham số.' },
    ],
    success: 'Chính xác! Mục tiêu cốt lõi là thích nghi tri thức mới hiệu quả đồng thời giữ được tri thức đã có.',
    error: 'Chưa chính xác. Hãy chú ý đến sự cân bằng giữa học mới và giữ cũ.',
  },
  {
    id: 'cl-core-challenge',
    title: 'Nhận diện acquisition và forgetting',
    prompt: 'Trước khi update, model đạt 90% trên task cũ. Sau khi học task mới, model đạt 88% trên task mới nhưng chỉ còn 61% trên task cũ. Kết luận nào phù hợp nhất?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Model có acquisition tốt trên task mới, nhưng retention kém vì hiệu năng task cũ giảm 29 điểm phần trăm.', isCorrect: true },
      { id: 'opt2', label: 'Model không học được task mới vì accuracy chưa đạt 100%.' },
      { id: 'opt3', label: 'Model giữ nguyên năng lực cũ vì task mới đạt accuracy cao.' },
      { id: 'opt4', label: 'Không thể đánh giá forgetting nếu không biết kích thước model.' },
    ],
    success: 'Chính xác. Model đã tiếp thu task mới, nhưng mức giảm từ 90% xuống 61% trên task cũ cho thấy forgetting rõ rệt.',
    error: 'Hãy đánh giá riêng hai phía: hiệu năng trên task mới phản ánh acquisition, còn thay đổi trên task cũ phản ánh retention và forgetting.',
  },
  {
    id: 'lifelong-learning-analogy',
    title: 'Nhận diện quy trình Continual Learning',
    prompt: 'Quy trình nào thể hiện đầy đủ nhất mục tiêu của Continual Learning?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Fine-tune model chỉ trên dữ liệu mới, rồi chỉ kiểm tra hiệu năng của task mới.' },
      { id: 'opt2', label: 'Giữ nguyên model và đưa tài liệu mới vào context bằng RAG khi suy luận.' },
      { id: 'opt3', label: 'Huấn luyện một model mới từ đầu trên toàn bộ dữ liệu sau mỗi lần cập nhật.' },
      { id: 'opt4', label: 'Update model qua từng giai đoạn, rồi đánh giá cả năng lực mới lẫn những năng lực cũ cần giữ.', isCorrect: true },
    ],
    success: 'Chính xác. Một quy trình CL phải kiểm tra đồng thời model học được gì mới và giữ được gì sau mỗi giai đoạn update.',
    error: 'Hãy tìm quy trình vừa cập nhật model qua nhiều giai đoạn, vừa đo acquisition và retention thay vì chỉ đánh giá task mới.',
  },
  {
    id: 'method-choice',
    title: 'Baseline khi có toàn bộ lịch sử',
    prompt: 'Nếu mọi dữ liệu lịch sử đều hợp lệ, rẻ và được giữ lại, baseline nào nên được báo cáo?',
    mode: 'single',
    options: [
      { id: 'opt1', label: 'Chỉ dùng RAG để đưa dữ liệu lịch sử vào context mà không cập nhật model.' },
      { id: 'opt4', label: 'Joint retraining hoặc full replay để ước lượng mức trần khi được truy cập toàn bộ lịch sử.', isCorrect: true },
      { id: 'opt2', label: 'Chỉ fine-tune trên stage mới và không đánh giá lại các năng lực cũ.' },
      { id: 'opt3', label: 'Naive sequential fine-tuning vì nó sử dụng ít dữ liệu nhất ở mỗi stage.' },
    ],
    success: 'Full-history training cho biết mức trần khi ràng buộc memory được nới lỏng.',
    error: 'Chọn baseline tương ứng với quyền truy cập dữ liệu.',
  },
]} />
