export const lessonMetadata = {
  domainId: 'continual-learning-llm', id: 'continual-llm-research-frontiers-quiz', locale: 'vi', title: 'Quiz',
  headings: ['Lý thuyết', 'Efficient replay', 'Controllable memory', 'Custom preferences'], keywords: ['Quiz', 'Future Directions'],
  conceptIds: ['cl-theory', 'efficient-replay-future', 'controllable-memory-future', 'custom-preferences'], pageCount: 4,
}

<MdxQuiz id="continual-llm-research-frontiers-quiz" questions={[
  { id: 'cl-theory', title: 'Lý thuyết', mode: 'single', prompt: 'Vì sao parameter-distance bound hoặc task-discrepancy bound hiện có chưa đóng khoảng trống lý thuyết của continual LLM?', options: [
    { id: 'a', label: 'Vì các bound chỉ áp dụng khi model không có tham số.' },
    { id: 'b', label: 'Vì pre-training và quy mô lớn khiến việc chuyển trực tiếp các bảo đảm CL hiện có sang LLM chưa đủ cơ sở.', isCorrect: true },
    { id: 'c', label: 'Vì LLM không thể được đánh giá trên nhiều task.' },
    { id: 'd', label: 'Vì mọi bound đều yêu cầu replay toàn bộ history.' },
  ], success: 'Lý thuyết hiện có là nền tảng, chưa phải bảo đảm hoàn chỉnh cho dynamics của pre-trained LLM.', error: 'Phân biệt kết quả CL thông thường với điều đã được chứng minh cho LLM lớn.' },
  { id: 'efficient-replay-future', title: 'Efficient replay', mode: 'single', prompt: 'Hai paper minh họa efficient replay khác nhau ở tín hiệu chọn mẫu như thế nào?', options: [
    { id: 'a', label: 'Cả hai chỉ dùng mixing ratio cố định.' },
    { id: 'b', label: 'KPIG chọn theo model size; forecasting chọn theo độ dài sequence.' },
    { id: 'c', label: 'KPIG chọn theo information gain; forecasting chọn example được dự báo sẽ bị quên.', isCorrect: true },
    { id: 'd', label: 'KPIG chọn ngẫu nhiên; forecasting replay toàn bộ upstream corpus.' },
  ], success: 'Một hướng đo giá trị thông tin, hướng kia dự báo trực tiếp nguy cơ forgetting.', error: 'Xác định tín hiệu quyết định sample nào đi vào replay.' },
  { id: 'controllable-memory-future', title: 'Controllable memory', mode: 'single', prompt: 'Điểm nào phân biệt controllable memory với việc chỉ tăng capacity của model?', options: [
    { id: 'a', label: 'Knowledge có các thao tác explicit như write, read, sequential update và selective forgetting.', isCorrect: true },
    { id: 'b', label: 'Mọi knowledge luôn được ghi vĩnh viễn vào toàn bộ weights.' },
    { id: 'c', label: 'Memory chỉ làm context window dài hơn nhưng không thể cập nhật.' },
    { id: 'd', label: 'Model không còn cần kiểm tra locality hoặc retention.' },
  ], success: 'Khả năng thao tác có chủ đích là lý do external memory đáng quan tâm.', error: 'Tập trung vào mức độ quan sát và điều khiển knowledge.' },
  { id: 'custom-preferences', title: 'Custom preferences', mode: 'single', prompt: 'Kết luận nào bám đúng bằng chứng hiện có về IBCL?', options: [
    { id: 'a', label: 'Posterior knowledge base chỉ tạo được một trade-off cố định, nên mỗi preference vẫn cần train lại knowledge base.' },
    { id: 'b', label: 'Bảo đảm dưới các giả định của IBCL đủ để suy ra cùng kết quả khi scale lên production LLM.' },
    { id: 'c', label: 'Kết quả personalized alignment ở setup được thử xác nhận preference mới không làm thay đổi retention.' },
    { id: 'd', label: 'IBCL tạo model cho nhiều trade-off từ posterior knowledge base, nhưng bằng chứng còn giới hạn về scale và phụ thuộc giả định.', isCorrect: true },
  ], success: 'Đây là hướng mở có bảo đảm dưới giả định, chưa phải kết quả phổ quát cho LLM lớn.', error: 'Giữ đồng thời cơ chế của IBCL và giới hạn empirical scale.' },
]} />
