export const lessonMetadata = {
  domainId: 'continual-learning-llm',
  id: 'regularization-overview',
  locale: 'vi',
  title: 'Regularization',
  headings: [
    'Giữ tri thức cũ trong objective',
    'Trade-off và ranh giới storage',
    'Hai không gian ràng buộc Weight và Function regularization',
  ],
  keywords: ['Regularization', 'Stability–Plasticity', 'Weight Regularization', 'Function Regularization'],
  conceptIds: ['regularization-family-objective', 'regularization-storage-tradeoff', 'regularization-space-split'],
  pageCount: 3,
}

<MdxPage page={0}>

## Giữ tri thức cũ trong objective

<ConceptHierarchy
  ariaLabel="Nhắc lại ba hướng tiếp cận chính, với Regularization là trọng tâm hiện tại"
  root={{ title: 'Các hướng tiếp cận chính' }}
  children={[
    { title: 'Replay', tone: 'blue', visual: 'database', muted: true },
    { title: 'Regularization', tone: 'amber', visual: 'two-term-loss' },
    { title: 'Architecture', tone: 'teal', visual: 'neural-network', muted: true },
  ]}
/>

Khi học Task B sau Task A theo cách tuần tự thông thường, hàm mục tiêu chỉ đơn thuần tối ưu trên dữ liệu của task mới:

<BlockMath formula="\mathcal{L}_{\mathrm{total}}=\mathcal{L}_{\mathrm{new}}" />

Vì objective chỉ quan tâm đến Task B, các bước cập nhật gradient sẽ tự do dịch chuyển trọng số mà không màng tới Task A. Đây chính là nguyên nhân trực tiếp làm xô lệch các vùng tham số cũ và gây ra **catastrophic forgetting**.

Để giải quyết vấn đề này mà không cần lưu trữ hay replay raw data cũ (vốn tốn storage và vướng rào cản privacy), hàm mục tiêu cần dung hòa đồng thời hai mục tiêu:
- **Term đầu (<InlineMath formula="\mathcal{L}_{\mathrm{new}}" />):** Tối ưu có giám sát trên dữ liệu của task mới để model thích nghi và học hỏi (plasticity).
- **Term thứ hai (<InlineMath formula="\lambda\mathcal{L}_{\mathrm{retention}}" />):** Đảm nhiệm vai trò bảo tồn mà **không có dữ liệu cũ**, do đó phải mang tính chất **unsupervised** đối với task cũ.

<BlockMath formula="\mathcal{L}_{\mathrm{total}}=\mathcal{L}_{\mathrm{new}}+\lambda\mathcal{L}_{\mathrm{retention}}" />

Để term thứ hai hoạt động mà không cần raw data cũ, cách tự nhiên nhất là đo lường độ lệch so với trạng thái cũ của mô hình:
- Trực quan và đơn giản nhất là đo **khoảng cách Euclidean** (chuẩn L2) trực tiếp giữa hai bộ trọng số.
- Mở rộng hơn dưới góc nhìn xác suất, ta có thể đo độ lệch phân phối dự đoán thông qua **KL divergence** giữa mô hình hiện tại và mô hình cũ.

Tuy nhiên, việc ghép hai mục tiêu vào cùng một loss function dẫn đến một trade-off lớn: hai term có thể xung đột hướng đi (**gradient conflict**). Trong khi <InlineMath formula="\mathcal{L}_{\mathrm{new}}" /> kéo mô hình về cực tiểu của task mới, thì <InlineMath formula="\lambda\mathcal{L}_{\mathrm{retention}}" /> lại kéo giật ngược về trạng thái cũ, khiến quá trình tối ưu dễ rơi vào thế giằng co và khó hội tụ.

</MdxPage>

<MdxPage page={1}>

## Trade-off và ranh giới storage

Retention càng mạnh, model càng khó thay đổi để học Task B. Nếu
<InlineMath formula="\lambda" /> quá nhỏ, forgetting có thể tăng; nếu quá lớn,
plasticity có thể giảm. Đây là stability–plasticity trade-off mà
regularization-based approach điều khiển.

Regularization-based approach không nhất thiết phải replay raw data cũ,
nhưng cũng không đồng nghĩa với zero storage cost. Wang et al. lưu ý rằng hướng
này thường cần lưu một frozen copy của model cũ làm mốc tham chiếu <Cite paper="wang2024comprehensive" evidence="regularization-overview-03-wang2024comprehensive" />.

<LessonImage
  assetPath="continual-learning-llm/02-regularization-tradeoff-storage-doodle.png"
  alt="Bốn doodle card minh họa regularization với lambda thấp làm rơi tri thức cũ, lambda cân bằng giữ cả cũ và mới, lambda cao chặn task mới, và frozen checkpoint cung cấp mốc tham chiếu lưu trữ"
/>

</MdxPage>

<MdxPage page={2}>

## Hai không gian ràng buộc Weight và Function regularization

Từ ý tưởng can thiệp trực tiếp vào parameter space (Weight regularization) này, hàng loạt công trình kinh điển trong continual learning đã phát triển theo hướng này <Cite paper="shi2024continualSurvey" evidence="regularization-overview-01-shi2024continualSurvey" />:
- **Elastic Weight Consolidation (EWC):** sử dụng ma trận Fisher Information Matrix để đo lường mức độ nhạy cảm của từng trọng số đối với bài toán cũ.
- **Synaptic Intelligence (SI) & Memory Aware Synapses (MAS):** theo dõi quỹ đạo gradient hoặc độ nhạy output để ước lượng tầm quan trọng của từng tham số mà không cần dữ liệu cũ.

Tuy nhiên, hướng tiếp cận này nhanh chóng bộc lộ một hạn chế mang tính bản chất: **trọng số có thể rất giống nhau nhưng performance vẫn sụt giảm nghiêm trọng**. Trong các mạng nơ-ron sâu và LLM, do tính phi tuyến cực cao, một sự dịch chuyển nhỏ ở những chiều nhạy cảm cũng có thể làm sụp đổ hoàn toàn hành vi của mô hình. Ngược lại, việc cố neo giữ cứng nhắc hàng tỷ tham số vừa triệt tiêu tính linh hoạt (plasticity) cần thiết để học task mới, vừa tiêu tốn tài nguyên khổng lồ để tính toán và lưu trữ ma trận độ quan trọng.

Chính vì sự đứt gãy giữa không gian tham số và năng lực thực tế, các khảo sát continual learning <Cite paper="wang2024comprehensive" evidence="regularization-overview-02-wang2024comprehensive" /> chính thức chia nhóm regularization thành **hai hướng tiếp cận độc lập**:

<ConceptHierarchy
  ariaLabel="Regularization trong ba hướng tiếp cận chính, chia tiếp thành Weight và Function regularization"
  root={{ title: 'Các hướng tiếp cận chính' }}
  children={[
    { title: 'Replay', tone: 'blue', visual: 'database', muted: true },
    {
      title: 'Regularization',
      tone: 'amber',
      visual: 'two-term-loss',
      children: [
        { title: 'Weight regularization', tone: 'violet' },
        { title: 'Function regularization', tone: 'teal' },
      ],
    },
    { title: 'Architecture', tone: 'teal', visual: 'neural-network', muted: true },
  ]}
/>

<LessonImage
  assetPath="continual-learning-llm/Regularizationv3.jpg"
  alt="Minh họa regularization-based approach chia thành weight regularization, ràng buộc tham số của model cũ, và function regularization, ràng buộc hành vi của model cũ"
/>

Từ sự phân nhánh này, **Function regularization** (ràng buộc trong function space) xuất hiện như một lời giải trực diện cho hạn chế của Weight regularization:
Thay vì kiểm soát từng con số trọng số tĩnh, ta chuyển sự chú ý sang **hành vi đầu ra** của mô hình. Bằng cách sử dụng checkpoint cũ làm frozen teacher và áp dụng **knowledge distillation**, model mới chỉ bị phạt nếu các phân phối output (logits) hoặc biểu diễn đặc trưng bị lệch khỏi model cũ trên cùng một ngữ cảnh đầu vào. Nhờ đó, các trọng số có thể tự do di chuyển sang vùng tối ưu mới để thích nghi với task mới, miễn là hàm ánh xạ cốt lõi của task cũ vẫn được bảo toàn.

Đối chiếu lại với ranh giới storage và mốc tham chiếu:
- **Weight regularization** dùng mốc frozen copy đó cùng importance của tham số để đặt penalty.
- **Function regularization** dùng model đã học trước đó làm teacher. Teacher target có thể được tạo từ input mới, một phần input cũ, dữ liệu ngoài không nhãn hoặc dữ liệu sinh <Cite paper="wang2024comprehensive" evidence="regularization-overview-04-wang2024comprehensive" />. Vì vậy, hướng tiếp cận này cũng có thể kết hợp linh hoạt với replay <Cite paper="wang2024comprehensive" evidence="regularization-overview-05-wang2024comprehensive" />.

<LessonNote>
Các bài học và code lab tiếp theo sẽ đi sâu vào chi tiết cơ chế toán học, cách ước lượng ma trận và triển khai thực nghiệm code từng nhánh để bạn quan sát rõ hiện tượng giữ tri thức trong thực tế.
</LessonNote>

</MdxPage>
