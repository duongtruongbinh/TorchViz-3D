export const lessonMetadata = {
  domainId: 'continual-learning-llm', id: 'cpt-other-directions', locale: 'vi', title: 'Các hướng CPT khác',
  headings: ['Ưu tiên dữ liệu model còn xử lý kém'], keywords: ['CEM', 'RHO-1', 'IR-DRO', 'TimeLMs'],
  conceptIds: ['cem', 'rho-1', 'ir-dro', 'industrial-cpt', 'cpt-sustainability'],
}

## Ưu tiên dữ liệu model còn xử lý kém

CPT không chỉ dùng để thích nghi với distribution shift. Nó còn có thể **sửa hành vi và ưu tiên học từ những dữ liệu khó hoặc lỗi** thay vì phân bổ compute như nhau cho toàn bộ corpus.

### <Cite paper="zhao2024large" label="CEM — học tiếp từ những câu trả lời sai" />

CEM bắt đầu bằng việc đánh giá model, thu thập các câu model trả lời sai, rồi bổ sung knowledge liên quan đến những lỗi đó vào dữ liệu train tiếp. Đơn vị được ưu tiên ở đây là **example**: model sai ở đâu thì tập trung cải thiện ở đó.

<LessonImage
  assetPath="continual-learning-llm/03-cpt-cem-tradeoff.png"
  alt="Hai card ưu điểm và hạn chế của CEM: tập trung vào knowledge gap để cải thiện QA đa domain, nhưng cần bước đánh giá, dữ liệu bổ sung và bằng chứng hiện chủ yếu đến từ QA"
  caption="CEM tập trung compute vào lỗi đã quan sát được, đổi lại pipeline phải phát hiện đúng lỗi và tìm được knowledge bổ sung phù hợp."
/>

### <Cite paper="lin2024rho" label="RHO-1 — chọn token đáng học" />

RHO-1 đề xuất **Selective Language Modeling (SLM)**. Một reference model đã học từ dữ liệu chất lượng cao giúp chấm từng token; model đang train chỉ nhận loss từ những token có **excess loss** cao, tức là còn khó với model hiện tại nhưng phù hợp với distribution mục tiêu. Vì vậy, phương pháp không đơn giản giữ mọi token có perplexity cao: token khó do nhiễu vẫn có thể bị loại.

<LessonImage
  assetPath="continual-learning-llm/04-cpt-rho1-tradeoff.png"
  alt="Hai card ưu điểm và hạn chế của RHO-1: chọn token đáng học để tăng hiệu quả token, nhưng cần reference model và phụ thuộc distribution tham chiếu"
  caption="RHO-1 lọc loss ở token level; chất lượng lựa chọn phụ thuộc mạnh vào reference model và corpus chất lượng cao dùng để huấn luyện nó."
/>

### <Cite paper="chen2024take" label="IR-DRO — gán trọng số cho từng sequence" />

IR-DRO chuyển mức focus từ token sang **sequence**. Phương pháp gán trọng số lớn hơn cho các đoạn có loss cao vừa phải vì chúng thường còn chứa tín hiệu hữu ích; những sequence có loss cao nhất có thể là dữ liệu nhiễu hoặc quá phức tạp nên không được ưu tiên vô điều kiện.

<LessonImage
  assetPath="continual-learning-llm/05-cpt-ir-dro-tradeoff.png"
  alt="Hai card ưu điểm và hạn chế của IR-DRO: reweight sequence giàu thông tin để cải thiện nhiều benchmark, nhưng loss cao có thể đến từ nhiễu và kết quả chưa bao phủ mọi data stream"
  caption="IR-DRO ưu tiên sequence có tín hiệu hữu ích thay vì mặc định sequence loss cao nhất luôn tốt; khả năng tổng quát sang data stream mới vẫn cần kiểm chứng."
/>

### Ứng dụng công nghiệp — cập nhật theo dòng dữ liệu mới

Với dữ liệu biến đổi nhanh như tweet, <Cite paper="amba2021dynamic" label="Dynamic Language Models" /> cập nhật BERT bằng dynamic vocabulary expansion và subsampling để ưu tiên phần dữ liệu mới hữu ích. <Cite paper="loureiro2022timelms" label="TimeLMs" /> chọn một recipe đơn giản hơn: phát hành các model dựa trên RoBERTa được train tiếp mỗi ba tháng trên tweet mới, không thêm ràng buộc đặc biệt để giữ model gần checkpoint cũ.

<LessonImage
  assetPath="continual-learning-llm/06-cpt-industrial-updates-tradeoff.png"
  alt="Hai card ưu điểm và hạn chế của cập nhật công nghiệp: CPT định kỳ rẻ hơn train lại từ đầu, nhưng bằng chứng chủ yếu đến từ Twitter với BERT hoặc RoBERTa và vẫn cần compute cùng đánh giá lại"
  caption="Cập nhật định kỳ giảm nhu cầu rebuild model, nhưng không loại bỏ chi phí training và kiểm tra lại knowledge cũ sau mỗi đợt."
/>

### <Cite paper="attanasio2023worth" label="CPT có luôn đáng chi phí môi trường?" />

Không phải mọi data stream đều cần cập nhật model liên tục. Attanasio et al. không tìm thấy lợi ích downstream ổn định khi temporal adaptation các model tiếng Anh trên dữ liệu social media, trong khi continuous training tạo thêm phát thải CO₂. Finding này đặt ra câu hỏi **khi nào lợi ích cập nhật đủ lớn để bù chi phí**, chứ không chứng minh CPT luôn không cần thiết.

<LessonImage
  assetPath="continual-learning-llm/07-cpt-sustainability-tradeoff.png"
  alt="Hai card ưu điểm và hạn chế khi đánh giá sustainability của CPT: cân bằng hiệu quả task, compute và CO2 qua nhiều thời điểm, model và task, nhưng phạm vi hiện chủ yếu là English social media và benchmark temporal còn thiếu"
  caption="Quyết định CPT cần cân bằng lợi ích task với compute và phát thải; bằng chứng hiện tại vẫn bị giới hạn bởi phạm vi dữ liệu và benchmark temporal."
/>

Nhìn chung, survey xem CPT là một lựa chọn hiệu quả hơn **combine-and-retrain** trong nhiều bối cảnh <Cite paper="shi2024continualSurvey" locator="§4.1.3" />. Tuy nhiên, granularity cần ưu tiên—example, token hay sequence—và việc có nên cập nhật hay không đều phải dựa trên lỗi thực tế, data stream và chi phí của hệ thống.
