export const lessonMetadata = {
  domainId: 'continual-learning-llm', id: 'continual-instruction-tuning', locale: 'vi', title: 'Continual Instruction Tuning',
  headings: ['Học instruction mới mà không quên instruction cũ'], keywords: ['CIT', 'Replay', 'PEFT'],
  conceptIds: ['cit-goal', 'cit-replay', 'cit-architecture'],
}

## Học instruction mới mà không quên instruction cũ

Trong Continual Instruction Tuning (**CIT**), instruction data đến theo dòng. Model phải học instruction mới trong khi vẫn làm đúng instruction cũ.

<ComparisonMatrix ariaLabel="Các nhóm giải pháp CIT trong survey" columns={['Ví dụ', 'Cơ chế']} rows={[
  { label: 'Replay', values: ['CT0, KPIG, SSR', 'Giữ/chọn dữ liệu cũ hoặc sinh synthetic instances'] },
  { label: 'Replay + regularization', values: ['DynaInst, InstructionSpeak', 'Chọn động dữ liệu và thêm ràng buộc học'] },
  { label: 'PEFT/architecture', values: ['O-LoRA, SAPT', 'Tách subspace hoặc chọn PEFT block phù hợp'] },
]} />

Replay, regularization và architecture-based methods vẫn phổ biến vì đơn giản và hiệu quả, nhưng đánh đổi bằng chi phí lưu data, tham số hoặc GPU memory.

Nguồn: [Shi et al., §4.3.3](https://arxiv.org/html/2404.16789v3#S4.SS3.SSS3).
