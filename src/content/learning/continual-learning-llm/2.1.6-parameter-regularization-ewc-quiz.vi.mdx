export const lessonMetadata = {
  domainId: 'continual-learning-llm',
  id: 'parameter-regularization-ewc-quiz',
  locale: 'vi',
  title: 'Quiz',
  headings: ['Mục tiêu', 'Ma trận importance', 'Trade-off'],
  keywords: ['Quiz', 'Parameter Regularization'],
  conceptIds: ['regularization-objective', 'importance-matrix', 'regularization-tradeoff'],
  pageCount: 3,
}

<MdxQuiz id="parameter-regularization-ewc-quiz" questions={[
  {
    id: 'regularization-objective', title: 'Mục tiêu', mode: 'single',
    prompt: 'Parameter regularization thêm thành phần nào khi model học Task B?',
    options: [
      { id: 'a', label: 'Một penalty hạn chế model rời xa tham số cũ ở các hướng quan trọng.' , isCorrect: true },
      { id: 'b', label: 'Một task ID để chọn output head tại inference.' },
      { id: 'c', label: 'Toàn bộ dữ liệu Task A vào mọi batch của Task B.' },
      { id: 'd', label: 'Một module mới tách biệt cho mỗi sample của Task B.' },
    ],
    success: 'Penalty tạo ràng buộc bằng parameter space.', error: 'Phân biệt regularization với replay và architecture expansion.',
  },
  {
    id: 'importance-matrix', title: 'Ma trận importance', mode: 'single',
    prompt: 'Hai tham số quan trọng khác nhau với task cũ. Cách nào phạt chúng ở hai mức khác nhau mà không phải lưu toàn bộ ma trận tương quan?',
    options: [
      { id: 'a', label: 'Dùng Σ = I để mọi tham số nhận cùng một penalty.' },
      { id: 'b', label: 'Dùng Σ đường chéo, với mỗi tham số có một trọng số importance Ωᵢ riêng.', isCorrect: true },
      { id: 'c', label: 'Chỉ tăng λ, vì một scalar có thể đặt penalty riêng cho từng tham số.' },
      { id: 'd', label: 'Dùng Σ đầy đủ để lưu cả importance lẫn mọi tương quan giữa các cặp tham số.' },
    ],
    success: 'Đường chéo của Σ cho mỗi tham số một trọng số Ωᵢ riêng mà không phải lưu toàn bộ tương quan.', error: 'Σ = I mới là trường hợp mọi tham số bị phạt như nhau.',
  },
  {
    id: 'regularization-tradeoff', title: 'Trade-off', mode: 'single',
    prompt: 'Điều gì dễ xảy ra khi λ quá lớn?',
    options: [
      { id: 'a', label: 'Model luôn đạt zero forgetting và vẫn học Task B tối ưu.' },
      { id: 'b', label: 'Replay buffer tự động lớn hơn.' },
      { id: 'c', label: 'Task routing trở nên chính xác tuyệt đối.' },
      { id: 'd', label: 'Model giữ trạng thái cũ mạnh nhưng có thể underfit Task B.', isCorrect: true },
    ],
    success: 'Regularization quá mạnh làm giảm plasticity.', error: 'λ điều khiển mức ưu tiên giữ cũ so với học mới.',
  },
]} />
