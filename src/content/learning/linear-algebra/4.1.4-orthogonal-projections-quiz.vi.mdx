export const lessonMetadata = {
  domainId: 'linear-algebra',
  id: 'orthogonal-projections-quiz',
  locale: 'vi',
  title: 'Quiz',
  headings: ['Projection lên Vector', 'Residual', 'Projection Matrix'],
  keywords: ['projection', 'residual', 'projection matrix', 'quiz'],
  conceptIds: ['projection-vector-formula', 'projection-residual', 'projection-matrix'],
  pageCount: 3,
}

<MdxQuiz id="orthogonal-projections-quiz" questions={[
  {
    id: 'projection-vector-formula',
    title: 'Projection lên Vector',
    prompt: 'Nếu $q$ là một unit vector ($\\|q\\| = 1$), hình chiếu trực giao (orthogonal projection) của vector $b$ lên $q$ bằng gì?',
    mode: 'single',
    options: [
      { id: 'a', label: '$q + b$' },
      { id: 'b', label: '$(q^T b) q$', isCorrect: true },
      { id: 'c', label: '$\\|b\\|_2 q$ trong mọi trường hợp' },
      { id: 'd', label: '$q^T q$' },
    ],
    success: 'Đúng. Với unit vector, mẫu số $q^T q = 1$, nên công thức chiếu là $\\text{proj}_q(b) = (q^T b) q$.',
    error: 'Bắt đầu từ công thức projection tổng quát: $\\text{proj}_q(b) = \\frac{q^T b}{q^T q} q$.',
  },
  {
    id: 'projection-residual',
    title: 'Residual',
    prompt: 'Gọi $p$ là hình chiếu trực giao của $b$ lên không gian con $S$, và sai số $e = b - p$. Phát biểu nào sau đây đúng?',
    mode: 'single',
    options: [
      { id: 'a', label: '$e$ luôn bằng $p$.' },
      { id: 'b', label: '$e$ luôn thuộc không gian con $S$.' },
      { id: 'c', label: '$e$ trực giao với mọi vector trong $S$ ($e \\perp S$).', isCorrect: true },
      { id: 'd', label: '$e$ phải luôn có norm bằng 1.' },
    ],
    success: 'Đúng. Vector phần dư (residual) $e = b - p$ luôn vuông góc với không gian con được chiếu lên ($e \\perp S$).',
    error: 'Hình chiếu trực giao gần nhất được đặc trưng bởi điều kiện sai số trực giao $e \\perp S$.',
  },
  {
    id: 'projection-matrix',
    title: 'Projection Matrix',
    prompt: 'Với $P$ là một ma trận chiếu trực giao (orthogonal projection matrix), hệ thức nào sau đây luôn đúng (tính lũy đẳng - idempotency)?',
    mode: 'single',
    options: [
      { id: 'a', label: '$P^2 = 0$' },
      { id: 'b', label: '$P$ luôn bằng ma trận đơn vị $I$.' },
      { id: 'c', label: '$P$ luôn luôn khả nghịch ($P^{-1}$ luôn tồn tại).' },
      { id: 'd', label: '$P^2 = P$', isCorrect: true },
    ],
    success: 'Đúng. Chiếu lần thứ hai không làm thay đổi vector đã nằm trong không gian chiếu ($P^2 = P$).',
    error: 'Ma trận chiếu thỏa mãn tính chất lũy đẳng (idempotent): $P^2 = P$ và đối xứng $P^T = P$.',
  },
]} />
